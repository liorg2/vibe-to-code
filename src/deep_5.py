# -*- coding: utf-8 -*-
"""Depth pass 5: richer deep-dives and examples for Sides, Languages,
Testing, and Pull request & code review."""

DETAIL_FIX = {

# ============================== SIDES ==============================

"Client": {
 "en": "The client is the computer you do not control. A browser, a phone app, a curl command, a partner's script — anything that sends you a request. Every number it posts is a claim, not a fact. The user can open DevTools, change a hidden field, replay a request, and skip every animation you built. That is not a hacker scenario; it is the default physics of a machine that belongs to someone else. The interface is a courtesy. The request is the product. If a number can be changed in a text box or a JSON body, it is not a decision until the server says so — hidden fields, disabled buttons and client-side totals are the same class of thing: they slow down honest people and do nothing to anyone else.\n\nNorthline, a fashion retailer, shipped a weekend sale. The React checkout applied a 30% coupon and posted the final line: `{price_cents: 12600, discount_percent: 30}`. The API stored those numbers. At 09:12 on Black Friday a student opened the Network tab, replayed the same POST with `discount_percent: 100`, and paid ₪0 for a ₪180 coat. By 15:40, support had 847 zero-price orders and finance had a ₪312,000 hole. Nothing was broken. The form was pretty. The server had believed a percentage that came from a laptop in a dorm.\n\nWhen an agent builds you a form, ask one question: what happens if this request is sent with curl, not the form? The trap is validation that lives only in the frontend — a disabled button, a `max` on an input, a function named `validateCoupon` imported only by the page. Duplicate the rule on the server and make the client number a hint you recompute. If the AI put an API key, a cost, or a role in frontend code, it is public the moment you deploy.",
 "he": "הקליינט הוא המחשב שאין לך שליטה עליו. דפדפן, אפליקציה בטלפון, פקודת curl, סקריפט של שותף — כל דבר ששולח לך ריקווסט. כל מספר שהוא שולח הוא טענה, לא עובדה. המשתמש יכול לפתוח את DevTools, לשנות שדה מוסתר, לשחזר ריקווסט, ולדלג על כל אנימציה שבנית. זה לא תרחיש של האקר; זו הפיזיקה הרגילה של מכונה ששייכת למישהו אחר. הממשק הוא נימוס. הריקווסט הוא המוצר. אם מספר אפשר לשנות בתיבת טקסט או בגוף JSON, הוא לא החלטה עד שהשרת אומר שכן — שדות מוסתרים, כפתורים מנוטרלים וסכומים בצד הקליינט הם אותה משפחה: הם מאטים אנשים ישרים ולא עושים כלום לשום אדם אחר.\n\nנורת׳ליין, רשת אופנה, העלתה מבצע של סוף שבוע. הצ׳קאאוט בריאקט הפעיל קופון של 30% ושלח את השורה הסופית: `{price_cents: 12600, discount_percent: 30}`. ה-API שמר את המספרים האלה. ב-09:12 בשישי השחור סטודנט פתח את טאב הרשת, שיחזר את אותו POST עם `discount_percent: 100`, ושילם ₪0 על מעיל ב-₪180. עד 15:40 לתמיכה היו 847 הזמנות במחיר אפס ולכספים היה חור של ₪312,000. שום דבר לא היה שבור. הטופס היה יפה. השרת האמין לאחוז שהגיע מלפטופ במעונות.\n\nכשסוכן בונה לך טופס, תשאל שאלה אחת: מה קורה אם שולחים את הריקווסט הזה ב-curl, לא מהטופס? המלכודת היא ולידציה שחיה רק בפרונט — כפתור מנוטרל, `max` על שדה, פונקציה בשם `validateCoupon` שמיובאת רק בדף. תשכפל את החוק בשרת ותהפוך את המספר מהקליינט לרמז שאתה מחשב מחדש. אם ה-AI שם מפתח API, מחיר או תפקיד בקוד פרונט — זה פומבי מרגע הדיפלוי."},

"Server": {
 "en": "A server is a process that waits, on a machine you usually cannot touch, and answers many people from one pool of CPU, memory and file descriptors. Your laptop has 32GB and one user. The container has 512MB and whoever showed up. That gap is the whole subject. Code that is correct for you can still knock everyone else over, because on a server one slow request is not a private inconvenience — it is a slice of the only thread, the only connection pool, the only disk. Capacity is shared whether you designed for it or not; a laptop never teaches you that.\n\nCheckout at Parcelly hashed passwords with `bcrypt.hashSync` inside the request, cost factor 12. On a developer Mac it took 90ms and felt instant. In production, Node's single thread meant those 90ms froze every other shopper. During a 19:00 flash sale, 47 people clicked Pay together, p99 jumped from 180ms to 8.4 seconds, and 12% of carts were abandoned in eleven minutes. The logs showed no error. The health check was still 200. The process was simply busy being synchronous. Moving the hash to `bcrypt.hash` (async) brought p99 back to 210ms the same evening.\n\nWhen an agent writes server code, ask: what does this do under a hundred concurrent requests, and where is the timeout? The trap is anything that works on one laptop and holds a shared resource — `readFileSync`, a full-table load into an array, a Stripe call with no deadline, a connection taken from the pool and never released. Also look where secrets went. The server is the only place they are actually secret. If the generated handler reads an unbounded body or retries forever, one client can become everyone else's outage.",
 "he": "שרת הוא תהליך שמחכה, על מכונה שבדרך כלל אי אפשר לגעת בה, ועונה להרבה אנשים מתוך בריכה אחת של מעבד, זיכרון ו-file descriptors. ללפטופ שלך יש 32GB ומשתמש אחד. לקונטיינר יש 512MB ומי שהגיע. הפער הזה הוא כל הנושא. קוד שנכון אצלך עדיין יכול להפיל את כולם, כי בשרת ריקווסט איטי אחד הוא לא אי-נוחות פרטית — הוא פרוסה מהת׳רד היחיד, ממאגר החיבורים היחיד, מהדיסק היחיד. קיבולת משותפת בין אם תכננת לזה ובין אם לא; לפטופ אף פעם לא מלמד אותך את זה.\n\nהצ׳קאאוט ב-Parcelly עשה האש לסיסמאות עם `bcrypt.hashSync` בתוך הריקווסט, cost factor 12. על מק של מפתח זה לקח 90ms והרגיש מיידי. בפרודקשן, הת׳רד הבודד של Node אמר שאותן 90ms הקפיאו כל קונה אחר. במבצע בזק ב-19:00, 47 אנשים לחצו שלם יחד, ה-p99 קפץ מ-180ms ל-8.4 שניות, ו-12% מהעגלות ננטשו בתוך אחת עשרה דקות. הלוגים לא הראו שגיאה. בדיקת הבריאות עדיין החזירה 200. התהליך פשוט היה עסוק בלהיות סינכרוני. מעבר ל-`bcrypt.hash` (אסינכרוני) החזיר את ה-p99 ל-210ms באותו ערב.\n\nכשסוכן כותב קוד שרת, תשאל: מה זה עושה תחת מאה ריקווסטים במקביל, ואיפה הטיים-אאוט? המלכודת היא כל דבר שעובד על לפטופ אחד ותופס משאב משותף — `readFileSync`, טעינת טבלה שלמה למערך, קריאה ל-Stripe בלי דדליין, חיבור שנלקח מהבריכה ולא הוחזר. תסתכל גם לאן הלכו הסודות. השרת הוא המקום היחיד שבו הם באמת סודיים. אם המטפל שנוצר קורא גוף בלי תקרה או מנסה שוב לנצח, קליינט אחד יכול להפוך לתקלה של כולם."},

"Frontend": {
 "en": "The frontend is what a human looks at: layout, copy, buttons, empty states, the spinner, the number in the corner. It runs on their device, so it is the fastest thing to change and the easiest thing to screenshot. A pretty interface is not evidence that the number is right. It is evidence that someone styled a number. The data still came from somewhere, and that somewhere is the part of the review that actually matters. Polish makes a wrong number harder to doubt, which is why a pretty cart can hide a finance incident for days.\n\nShoply rebuilt the VAT line in the cart over a weekend. The component looked perfect in dark mode. A developer hardcoded `0.17` because \"Israel is 17%\" and the design used that figure in Figma. For eleven days, EU checkout at 21% and UK at 20% still showed 17%. Finance reconciled on the 12th and found a ₪41,200 shortfall against Stripe tax — 1,104 invoices, all looking correct on screen, all wrong in the ledger. Support had no tickets: customers were undercharged and did not complain. The beautiful cart was the reason nobody opened DevTools.\n\nWhen an agent hands you a UI, ignore the happy screenshot and ask where each displayed number is computed, and what the user sees when the request fails, is empty, or is still loading. The trap is business rules in the component — a price, a permission, a limit, a tax rate — because those are suggestions a user can edit. Keep the pretty path, then demand the three boring states in the same prompt. A spinner that never ends and a silent wrong total are both frontend bugs; only one of them looks like a bug.",
 "he": "הפרונט-אנד הוא מה שבן אדם מסתכל עליו: פריסה, טקסט, כפתורים, מצבים ריקים, הספינר, המספר בפינה. הוא רץ על המכשיר שלהם, אז זה הדבר הכי מהיר לשנות והכי קל לצלם. ממשק יפה אינו ראיה שהמספר נכון. הוא ראיה שמישהו עיצב מספר. הדאטה עדיין הגיע ממקום כלשהו, והמקום הזה הוא חלק הסקירה שחשוב באמת. ליטוש הופך מספר שגוי לקשה יותר לפקפק בו, ובגלל זה עגלה יפה יכולה להסתיר תקלת כספים ימים.\n\nשופלי בנה מחדש את שורת המע\"מ בעגלה בסוף שבוע. הקומפוננטה נראתה מושלמת במצב כהה. מפתח כתב `0.17` בקשיח כי ״בישראל זה 17%״ וזה המספר שהיה בפיגמה. במשך אחד עשר ימים, צ׳קאאוט באירופה ב-21% ובבריטניה ב-20% עדיין הציג 17%. הכספים עשו התאמה ב-12 לחודש ומצאו חוסר של ₪41,200 מול מס של Stripe — 1,104 חשבוניות, כולן נראות נכונות על המסך, כולן שגויות בספרים. לתמיכה לא היו פניות: לקוחות חויבו חסר ולא התלוננו. העגלה היפה הייתה הסיבה שאף אחד לא פתח DevTools.\n\nכשסוכן מוסר לך ממשק, תתעלם מצילום המסך המאושר ותשאל איפה מחושב כל מספר שמוצג, ומה המשתמש רואה כשהריקווסט נכשל, ריק, או עדיין טוען. המלכודת היא חוקים עסקיים בקומפוננטה — מחיר, הרשאה, מגבלה, שיעור מס — כי אלה הצעות שמשתמש יכול לערוך. תשאיר את הנתיב היפה, ואז תדרוש את שלושת המצבים המשעממים באותו פרומפט. ספינר שלא נגמר וסכום שגוי בשקט הם שניהם באגים בפרונט; רק אחד מהם נראה כמו באג."},

"Backend": {
 "en": "The backend is the part that remembers, decides and refuses. It talks to the database, to Stripe, to the warehouse. If the frontend vanished and someone spoke HTTP at your API, whatever they could still do is the real product. Limits, prices, who owns a row — those are backend facts or they are theatre. The till is not the shop window. Anything you only enforced by hiding a control was never enforced; partners, mobile apps and curl never saw the control.\n\nMerchDesk limited each seller to 50 product photos. The New Photo button hid itself at 50. `POST /photos` did not check. A partner integration, written against the docs and not the UI, uploaded 6,400 images onto a free account over a weekend. Disk on the uploads volume hit 98% at 04:11 on Monday; the API started returning 500 for every seller, including paying ones. Cleanup took fourteen hours and a conversation with a customer who had broken nothing. The quota was theatre until the API said no; partners never saw the button, and they should not have to. The ten-line quota check would have returned 403 and a sentence. It was never written, because the button already looked like a limit.\n\nWhen you prompt an agent, name the side: reject on the server with 403 when the quota is exceeded, and also hide the button. The trap is the visible half of the rule. Also keep money, identity and secrets here — a Stripe key in a React file is a bill. A useful review question: if I delete the frontend tomorrow, what can a logged-in client still create, update or read? Whatever you cannot say no to in the API is already in production, whether or not a button exists.",
 "he": "הבק-אנד הוא החלק שזוכר, מחליט ומסרב. הוא מדבר עם הדאטהבייס, עם Stripe, עם המחסן. אם הפרונט היה נעלם ומישהו היה מדבר HTTP אל ה-API שלך, מה שהוא עדיין יכול לעשות הוא המוצר האמיתי. מגבלות, מחירים, מי הבעלים של שורה — אלה עובדות בק-אנד או שהם תיאטרון. הקופה היא לא חלון הראווה. כל דבר שנאכף רק בהסתרת פקד מעולם לא נאכף; שותפים, אפליקציות מובייל ו-curl מעולם לא ראו את הפקד.\n\nמרצ׳דסק הגביל כל מוכר ל-50 תמונות מוצר. כפתור תמונה חדשה הסתיר את עצמו ב-50. `POST /photos` לא בדק. אינטגרציה של שותף, שנכתבה מול התיעוד ולא מול הממשק, העלתה 6,400 תמונות לחשבון חינם בסוף שבוע. הדיסק בווליום ההעלאות הגיע ל-98% ב-04:11 ביום שני; ה-API התחיל להחזיר 500 לכל מוכר, כולל משלמים. הניקוי לקח ארבע עשרה שעות ושיחה עם לקוח שלא שבר כלום. המכסה הייתה תיאטרון עד שה-API אמר לא; שותפים מעולם לא ראו את הכפתור, וגם לא היו צריכים. בדיקת המכסה של עשר שורות הייתה מחזירה 403 ומשפט. היא מעולם לא נכתבה, כי הכפתור כבר נראה כמו מגבלה.\n\nכשאתה מפרומפט סוכן, תן שם לצד: לדחות בשרת עם 403 כשהמכסה חורגת, וגם להסתיר את הכפתור. המלכודת היא החצי הגלוי של החוק. תשאיר גם כסף, זהות וסודות כאן — מפתח Stripe בקובץ ריאקט הוא חשבון. שאלת סקירה שימושית: אם אמחק את הפרונט מחר, מה קליינט מחובר עדיין יכול ליצור, לעדכן או לקרוא? כל מה שאי אפשר להגיד לו לא ב-API כבר נמצא בפרודקשן, בין אם יש כפתור ובין אם לא."},

"API": {
 "en": "An API is a contract between two programs: the requests one may send, the shape that comes back, and the failures that are allowed. It is not a website and it is not magic. Your database driver is an API. Stripe is an API. The danger is that someone else builds against the contract, so changing a field name is no longer a private refactor. The other danger is that the happy path is three lines and the rest of the work is timeouts, 429s, and bodies the docs never showed. Integrations fail in the unhappy path, every time, and that path is where the money is.\n\nFold shipped checkout against Stripe's test mode in four hours. Sandbox answers in 80ms. In production they called `PaymentIntent.create` on API version `2020-08-27` because that string was in an old snippet. Stripe had moved the default to `2024-11-20`, where `automatic_payment_methods` is required. For four hours on a Sunday, 23% of EU cards returned a shape the code did not parse; the handler treated it as success and wrote `orders.status = 'paid'`. 64 orders, ₪18,400, never actually captured. Stripe's dashboard showed `incomplete`. The shop showed a thank-you page.\n\nWhen someone says integrate X, translate it to: call X's API and handle its failures. Paste the current docs, not a memory of 2022. Ask three questions of the generated client: what happens on a 2-second timeout, what happens on 429, and what happens if a field you need is missing. Then find the key. If it is in the repo, in the frontend, or in a log line, it is already burned — rotate it. Invented field names are the most common agent failure, and they compile, and they look right.",
 "he": "API הוא חוזה בין שתי תוכניות: אילו ריקווסטים מותר לשלוח, איזו צורה חוזרת, ואילו כשלים מותרים. זה לא אתר וזה לא קסם. הדרייבר של הדאטהבייס שלך הוא API. Stripe הוא API. הסכנה היא שמישהו אחר בונה מול החוזה, אז שינוי שם שדה כבר אינו ריפקטור פרטי. הסכנה השנייה היא שנתיב השמחה הוא שלוש שורות ושאר העבודה היא טיים-אאוטים, 429, וגופים שהתיעוד מעולם לא הציג. אינטגרציות נכשלות בנתיב הלא-מאושר, תמיד, ושם נמצא הכסף.\n\nFold העלו צ׳קאאוט מול מצב הטסט של Stripe בארבע שעות. הסנדבוקס עונה ב-80ms. בפרודקשן הם קראו ל-`PaymentIntent.create` בגרסת API `2020-08-27` כי זו המחרוזת שהייתה בקטע ישן. Stripe העבירו את ברירת המחדל ל-`2024-11-20`, שם `automatic_payment_methods` חובה. במשך ארבע שעות ביום ראשון, 23% מהכרטיסים באירופה החזירו צורה שהקוד לא פירסר; המטפל התייחס לזה כהצלחה וכתב `orders.status = 'paid'`. 64 הזמנות, ₪18,400, מעולם לא נלכדו באמת. הדשבורד של Stripe הראה `incomplete`. החנות הראתה דף תודה.\n\nכשמישהו אומר לחבר את X, תתרגם ל: לקרוא ל-API של X ולטפל בכשלים שלו. תדביק את התיעוד הנוכחי, לא זיכרון מ-2022. תשאל שלוש שאלות על הקליינט שנוצר: מה קורה בטיים-אאוט של שתי שניות, מה קורה ב-429, ומה קורה אם שדה שאתה צריך חסר. ואז תמצא את המפתח. אם הוא בריפו, בפרונט או בשורת לוג — הוא כבר שרוף, תחליף אותו. שמות שדות מומצאים הם כשל הסוכן הנפוץ ביותר, והם מתקמפלים, והם נראים נכון."},

"localhost & port": {
 "en": "localhost is a name that always means this machine, and it never travels. A port is a numbered door on that machine so more than one program can listen at once — 3000 for the app, 5432 for Postgres, 6379 for Redis. A URL is three pieces: host, door, path. The confusion is always the same: which machine, and which door, from whose point of view. Inside a container, localhost is the container. On your laptop, it is your laptop. In a production browser, it is the customer's laptop, where your API is not running.\n\nLens Shop built the storefront with `VITE_API_URL=http://localhost:3000`. It worked for six weeks on every developer machine. The production JavaScript bundle baked that string in at build time. After the launch email to 2,400 customers, the browser on each of their phones tried to open port 3000 on the phone itself and got connection refused. Support volume hit 180 tickets in 40 minutes. The API on the real host was healthy the entire time — nobody in the office could reproduce it, because their laptops really did have something on 3000. One environment variable, one build-time inlining, one afternoon of thinking the internet was down.\n\nWhen an agent writes a base URL, read it. If it says localhost and the code will run in a browser or on a server you do not sit at, it is wrong. The trap is config that works in `.env` locally and is compiled into the frontend, plus the cousin bug: yesterday's process still bound to 3000 so today's server silently moved to 3001. Know how to see who owns a port, and know that binding to 127.0.0.1 inside Docker publishes nothing. Binding a dev server to 0.0.0.0 on café wifi is the other direction of the same mistake.",
 "he": "localhost הוא שם שתמיד אומר המכונה הזאת, והוא אף פעם לא נוסע. פורט הוא דלת ממוספרת במכונה כדי שיותר מתוכנית אחת תוכל להאזין בבת אחת — 3000 לאפליקציה, 5432 לפוסטגרס, 6379 ל-Redis. URL הוא שלושה חלקים: מארח, דלת, נתיב. הבלבול תמיד אותו דבר: איזו מכונה, ואיזו דלת, מנקודת המבט של מי. בתוך קונטיינר, localhost הוא הקונטיינר. על הלפטופ שלך, הוא הלפטופ. בדפדפן בפרודקשן, הוא הלפטופ של הלקוח, שבו ה-API שלך לא רץ.\n\nלנס שופ בנו את חזית החנות עם `VITE_API_URL=http://localhost:3000`. זה עבד שישה שבועות על כל מכונת מפתח. באנדל הג׳אווהסקריפט בפרודקשן אפה את המחרוזת הזאת בזמן בילד. אחרי מייל ההשקה ל-2,400 לקוחות, הדפדפן בכל טלפון ניסה לפתוח פורט 3000 על הטלפון עצמו וקיבל connection refused. נפח התמיכה הגיע ל-180 פניות ב-40 דקות. ה-API על המארח האמיתי היה בריא כל הזמן — אף אחד במשרד לא הצליח לשחזר, כי אצלם באמת היה משהו על 3000. משתנה סביבה אחד, הטמעה בזמן בילד, אחר צהריים של לחשוב שהאינטרנט נפל.\n\nכשסוכן כותב כתובת בסיס, תקרא אותה. אם כתוב localhost והקוד ירוץ בדפדפן או על שרת שאתה לא יושב לידו, זה שגוי. המלכודת היא קונפיג שעובד ב-`.env` מקומית ועובר קומפילציה לפרונט, ועוד באג בן דוד: התהליך של אתמול עדיין תפוס על 3000 אז השרת של היום עבר בשקט ל-3001. תדע איך לראות מי מחזיק פורט, ותדע שקשירה ל-127.0.0.1 בתוך Docker לא מפרסמת כלום. לקשור שרת פיתוח ל-0.0.0.0 בוויי-פיי של בית קפה הוא הכיוון ההפוך של אותה טעות."},

"DNS": {
 "en": "DNS is a phone book with a delay. You ask what shop.example.com is, a resolver answers with an IP or another name, and then caches that answer for a lifetime you set — the TTL. Changing the record does not change the copies already sitting in ISPs, phones, and coffee-shop routers. Those copies expire on their own schedule. Until they do, part of the world is still knocking on the old door, and they will write orders into whatever is still listening there.\n\nOn a Sunday at 21:00, Braid moved checkout from a VM at 34.120.5.9 to CloudFront. They updated the A record, refreshed, saw the new site, and went home. The previous TTL was 43,200 seconds — twelve hours. Until 09:00 Monday, roughly 31% of sessions still hit the decommissioned VM. That VM had been left running \"just in case\" with yesterday's database. 190 paid orders landed in a Postgres nobody was replicating. The new site showed empty carts for those customers. The team only believed the reports after a `dig +trace` from a phone on cellular showed the old IP and office wifi showed the new one.\n\nBefore you debug application code for \"some users see the old version\", resolve the name from somewhere that is not your laptop. The trap an agent walks you into is an A record aimed at an ephemeral IP, where a CNAME at a stable hostname was required — it works for a week, then the load balancer moves and half the internet cannot find you. Lower the TTL to 300 a day before a cutover. After a cutover, leave the old origin up in read-only until the TTL has fully expired, or you will replay this story with a different domain.",
 "he": "DNS הוא ספר טלפונים עם השהיה. אתה שואל מה זה shop.example.com, רזולבר עונה עם IP או שם אחר, ואז שומר את התשובה בקאש למשך חיים שאתה קובע — ה-TTL. שינוי הרשומה לא משנה את העותקים שכבר יושבים אצל ספקיות, בטלפונים ובנתבים של בתי קפה. העותקים האלה פגים בלוח זמנים משלהם. עד אז, חלק מהעולם עדיין דופק בדלת הישנה, והם יכתבו הזמנות אל מה שעדיין מאזין שם.\n\nביום ראשון ב-21:00, ברייד העבירו צ׳קאאוט ממכונה ב-34.120.5.9 ל-CloudFront. הם עדכנו את רשומת ה-A, ריעננו, ראו את האתר החדש, והלכו הביתה. ה-TTL הקודם היה 43,200 שניות — שתים עשרה שעות. עד 09:00 ביום שני, בערך 31% מהסשנים עדיין פגעו במכונה שכובתה. המכונה ההיא נשארה דולקת ״ליתר ביטחון״ עם הדאטהבייס של אתמול. 190 הזמנות ששולמו נחתו בפוסטגרס שאף אחד לא שיכפל. האתר החדש הציג עגלות ריקות לאותם לקוחות. הצוות האמין לדיווחים רק אחרי ש-`dig +trace` מטלפון בסלולר הראה את ה-IP הישן והוויי-פיי במשרד הראה את החדש.\n\nלפני שמדבגים קוד אפליקציה על ״חלק מהמשתמשים רואים את הגרסה הישנה״, תפתור את השם ממקום שהוא לא הלפטופ שלך. המלכודת שסוכן מכניס אותך אליה היא רשומת A שמכוונת ל-IP זמני, במקום CNAME לשם מארח יציב — זה עובד שבוע, ואז מאזן העומסים זז וחצי מהאינטרנט לא מוצא אותך. תוריד את ה-TTL ל-300 יום לפני מעבר. אחרי מעבר, תשאיר את המקור הישן ב-read-only עד שה-TTL פג לגמרי, או שתשחזר את הסיפור הזה עם דומיין אחר."},

"Full stack": {
 "en": "Full stack means following one value across every layer until you can say where it became wrong: the button, the client bundle, the HTTP call, the handler, the query, the row, the webhook that wrote the row last month. The stack is just the list of choices — language, framework, database, host — and naming them lets you ask what each one is for. The person who can walk a number from the screen to the disk is the person who closes the ticket. Everyone else describes a symptom and waits.\n\nBlack Friday at Knitco, a 30% sale. The React cart applied 30%. The API applied 30% again on `unit_price`. Stripe was given a coupon `BF30` as well. Three discounts stacked. Average order was 73% off instead of 30%. In six hours they took 2,140 orders and recognised ₪89,000 of revenue against ₪241,000 of goods at cost. The frontend team blamed Stripe. The backend team blamed the coupon config. Nobody followed one SKU through the three layers until an on-call engineer printed the same `order_id` from the browser payload, the API log, and the Stripe event. The bug was not in one place. It was the absence of a single owner for \"what is the price\".\n\nYou already operate at every layer when you build with an agent, usually without depth in any of them. Aim learning at the seams: how a value travels, where it can be changed, who is allowed to change it. When an agent adds a new piece to the stack — another cache, another framework, another queue — ask what it replaces and who gets the pager at 03:00. The trap is confidence in the visible layer and hand-waving underneath, where money and identity live. One source of truth for a price, a permission, a stock count. If you find three, you have already found the next incident.",
 "he": "פול סטאק אומר לעקוב אחרי ערך אחד בכל שכבה עד שאפשר להגיד איפה הוא נהיה שגוי: הכפתור, הבאנדל בקליינט, קריאת ה-HTTP, המטפל, השאילתה, השורה, ה-webhook שכתב את השורה בחודש שעבר. הסטאק הוא פשוט רשימת הבחירות — שפה, פריימוורק, דאטהבייס, הוסט — ולתת להן שמות מאפשר לשאול בשביל מה כל אחת. מי שיודע להוליך מספר מהמסך לדיסק הוא מי שסוגר את הכרטיס. כל השאר מתארים סימפטום ומחכים.\n\nשישי השחור בקינטקו, מבצע 30%. עגלת הריאקט הפעילה 30%. ה-API הפעיל 30% שוב על `unit_price`. ל-Stripe ניתן גם קופון `BF30`. שלוש הנחות נערמו. הזמנה ממוצעת הייתה 73% הנחה במקום 30%. בשש שעות הם לקחו 2,140 הזמנות והכירו ב-₪89,000 הכנסות מול ₪241,000 עלות סחורה. צוות הפרונט האשים את Stripe. צוות הבק-אנד האשים את קונפיג הקופון. אף אחד לא עקב אחרי SKU אחד בשלוש השכבות עד שמהנדס כונן הדפיס את אותו `order_id` מפיילוד הדפדפן, מלוג ה-API ומאירוע Stripe. הבאג לא היה במקום אחד. הוא היה היעדר בעלים יחיד ל״מה המחיר״.\n\nאתה כבר פועל בכל שכבה כשאתה בונה עם סוכן, בדרך כלל בלי עומק באף אחת מהן. תכוון למידה לתפרים: איך ערך נוסע, איפה אפשר לשנות אותו, למי מותר לשנות אותו. כשסוכן מוסיף חתיכה חדשה לסטאק — עוד קאש, עוד פריימוורק, עוד תור — תשאל מה היא מחליפה ומי מקבל את הפאג׳ר ב-03:00. המלכודת היא ביטחון בשכבה הגלויה וטשטוש מתחת, שם חיים כסף וזהות. מקור אמת אחד למחיר, להרשאה, לכמות מלאי. אם מצאת שלושה, כבר מצאת את התקלה הבאה."},

"DevTools": {
 "en": "DevTools is the difference between guessing and looking. Every browser ships a panel — F12, or Ctrl+Shift+I — that shows the live DOM, the errors, every request, the cookies, and the running JavaScript paused mid-sentence. The page is no longer a poster you stare at. It is a program you can interrogate. Most frontend arguments die in ten seconds here, which is why teams that skip it spend days in Slack describing colours and spinner timings they could have read off a timing waterfall. Looking is cheaper than asking a model to guess which layer is slow.\n\nHarbor's new PDP felt \"slow\" for three days. The backend team tuned Postgres, added an index on `products.slug`, and cut the API from 40ms to 12ms. Users still complained. An engineer opened DevTools on a mid-range Android: Network showed TTFB 12ms and a 2.8 second gap after JavaScript downloaded, all in Scripting, none in Waiting. React was rendering a 900-row related-products table in one go. The API was innocent. The index was irrelevant. The waterfall had been sitting there the whole time, on every laptop, behind a keyboard shortcut nobody had pressed.\n\nWhen an agent says the frontend is slow, or a request failed, or a button does nothing, open the panel before you prompt again. Paste a screenshot of the Network row and the Console error, not a description. The trap is asking the model to \"fix the UI\" while the evidence is already in the browser — you will get a rewrite of CSS that does not touch the 2.8s render. Learn where five things live: Elements, Console, Network, Application, Sources. That afternoon of orientation pays back every time an AI confidently blames the wrong layer.",
 "he": "DevTools הוא ההבדל בין לנחש לבין להסתכל. כל דפדפן מגיע עם פאנל — F12, או Ctrl+Shift+I — שמראה את ה-DOM החי, את השגיאות, כל ריקווסט, את הקוקיז, ואת הג׳אווהסקריפט הרץ עצור באמצע משפט. הדף כבר אינו פוסטר שמביטים בו. הוא תוכנית שאפשר לחקור. רוב הוויכוחים בפרונט מתים כאן בעשר שניות, ובגלל זה צוותים שמדלגים על זה מבלים ימים בסלאק בתיאור צבעים וזמני ספינר שאפשר היה לקרוא ממפל תזמונים. להסתכל זול יותר מלבקש ממודל לנחש איזו שכבה איטית.\n\nה-PDP החדש של הארבור הרגיש ״איטי״ שלושה ימים. צוות הבק-אנד כיוון פוסטגרס, הוסיף אינדקס על `products.slug`, וחתך את ה-API מ-40ms ל-12ms. משתמשים עדיין התלוננו. מהנדס פתח DevTools על אנדרואיד בינוני: Network הראה TTFB של 12ms ופער של 2.8 שניות אחרי שהג׳אווהסקריפט ירד, הכל ב-Scripting, כלום ב-Waiting. ריאקט רינדר טבלת מוצרים קשורים של 900 שורות בבת אחת. ה-API היה חף מפשע. האינדקס היה לא רלוונטי. המפל ישב שם כל הזמן, על כל לפטופ, מאחורי קיצור מקלדת שאף אחד לא לחץ.\n\nכשסוכן אומר שהפרונט איטי, או שריקווסט נכשל, או שכפתור לא עושה כלום, תפתח את הפאנל לפני שאתה מפרומפט שוב. תדביק צילום של שורת Network ושל שגיאת הקונסול, לא תיאור. המלכודת היא לבקש מהמודל ״לתקן את ה-UI״ בזמן שהראיה כבר בדפדפן — תקבל שכתוב CSS שלא נוגע ברינדור של 2.8 שניות. תלמד איפה חמישה דברים חיים: Elements, Console, Network, Application, Sources. אחר הצהריים הזה של התמצאות מחזיר את עצמו בכל פעם ש-AI מאשים בביטחון את השכבה הלא נכונה."},

"The Network tab": {
 "en": "The Network tab is the wire, written down. Every request the page made, with the real method, the real status, the real headers, the real body, and how many milliseconds were spent waiting versus downloading. Your code has a story about what it sent. This panel has the recording. When the two disagree, the recording wins. Filter to Fetch/XHR, disable cache, preserve log across redirects — three checkboxes that turn a mystery into a row you can click.\n\nAt 11:40 on a Wednesday, Oak & Co checkout started failing for a partner storefront on a new domain. The frontend showed a generic 500. Backend logs were empty: no request had arrived. Six hours of API archaeology later, someone opened Network, clicked the red row, and saw status `(failed)` with a CORS error in the Console — the POST never left the browser. The preflight OPTIONS got 204 from an old `Access-Control-Allow-Origin` that listed only `https://shop.oak.co`. The new origin `https://b2b.oak.co` was not on the list. One header on the server. The 500 was a lie the UI invented because `fetch` threw.\n\nWhen an agent cannot explain a failed call, paste the Headers, Payload, Response and Timing of that one row — or Copy as cURL and run it yourself. Ask: did it leave the browser, and what was the status? The trap is debugging the server for a request that never arrived, or blaming \"the network\" when Timing shows 1.19s Waiting (TTFB) and 0.05s download — that is your process, not the user's wifi. Also read Cache-Control on the response before you assume a deploy did nothing. The tab is where HTTP, CORS, auth headers and slowness stop being vocabulary and become a screenshot.",
 "he": "טאב הרשת הוא החוט, כתוב. כל ריקווסט שהדף שלח, עם המתודה האמיתית, הסטטוס האמיתי, ההדרים האמיתיים, הגוף האמיתי, וכמה מילישניות הלכו על המתנה מול הורדה. לקוד שלך יש סיפור על מה שהוא שלח. לפאנל הזה יש הקלטה. כששניהם לא מסכימים, ההקלטה מנצחת. סינון ל-Fetch/XHR, כיבוי קאש, שמירת לוג לאורך הפניות — שלוש תיבות שמחליפות תעלומה בשורה שאפשר ללחוץ עליה.\n\nב-11:40 ביום רביעי, הצ׳קאאוט של אוק אנד קו התחיל להיכשל לחזית שותף על דומיין חדש. הפרונט הציג 500 גנרי. לוגי הבק-אנד היו ריקים: ריקווסט לא הגיע. אחרי שש שעות של ארכיאולוגיה ב-API, מישהו פתח Network, לחץ על השורה האדומה, וראה סטטוס `(failed)` עם שגיאת CORS בקונסול — ה-POST מעולם לא יצא מהדפדפן. ה-preflight OPTIONS קיבל 204 מ-`Access-Control-Allow-Origin` ישן שרשם רק `https://shop.oak.co`. המקור החדש `https://b2b.oak.co` לא היה ברשימה. הדר אחד בשרת. ה-500 היה שקר שה-UI המציא כי `fetch` זרק.\n\nכשסוכן לא מצליח להסביר קריאה שנכשלה, תדביק את Headers, Payload, Response ו-Timing של אותה שורה — או Copy as cURL ותריץ בעצמך. תשאל: האם זה יצא מהדפדפן, ומה היה הסטטוס? המלכודת היא לדבג את השרת על ריקווסט שמעולם לא הגיע, או להאשים ״את הרשת״ כש-Timing מראה 1.19 שניות Waiting (TTFB) ו-0.05 שניות הורדה — זה התהליך שלך, לא הוויי-פיי של המשתמש. תקרא גם Cache-Control ברספונס לפני שאתה מניח שדיפלוי לא עשה כלום. הטאב הוא המקום שבו HTTP, CORS, הדרי אימות ואיטיות מפסיקים להיות אוצר מילים והופכים לצילום מסך."},

"Console & breakpoints": {
 "en": "The console is a REPL on the live page: errors appear here, and you can type expressions against the actual DOM and the actual store. A breakpoint is a pause button on a line of JavaScript. Time stops. You read `order.qty` as it really is, not as a log line you planned in advance. Logging is guessing which values you will care about. Breaking is looking at all of them, plus the call stack that shows how you got here. Conditional breakpoints — pause only when `order.id === '7c3f'` — turn a 10,000-request haystack into one freeze.\n\nAt 16:05, after a deploy, Cinder checkout threw `Cannot read properties of undefined (reading 'id')` for about 8% of sessions. The agent added optional chaining in three files; the 500 became a silent empty cart. A breakpoint on `getOwner` showed `cart.items` was `[]` even though the user had four things on screen. Application tab: `localStorage` key had been renamed from `cart_v2` to `cart_v3` in the same deploy, with no migration. 40 minutes of traffic, 220 carts wiped at pay time, ₪61,000 of intended GMV sitting in a key nobody read. The console had the TypeError from the first minute. Nobody opened it until support volume forced them.\n\nWhen an agent wants to sprinkle `console.log` through a function, put a breakpoint there instead and paste the real values into the prompt. Ask: what is this variable right now, on the failing order? The trap is optional chaining as a fix — `cart.items?.id` makes the crash go away and leaves the empty-cart bug in production as a quiet wrong answer. Also watch for `debugger` statements the model leaves behind, and for error handlers that `console.error` and swallow. A red stack in the console is a gift. A page that just sits there is the expensive kind.",
 "he": "הקונסול הוא REPL על הדף החי: שגיאות מופיעות כאן, ואפשר להקליד ביטויים מול ה-DOM האמיתי והסטור האמיתי. נקודת עצירה היא כפתור השהיה על שורת ג׳אווהסקריפט. הזמן נעצר. אתה קורא את `order.qty` כמו שהוא באמת, לא כמו שורת לוג שתכננת מראש. לוג הוא ניחוש אילו ערכים יעניינו אותך. עצירה היא להסתכל על כולם, ועוד על מחסנית הקריאות שמראה איך הגעת לכאן. נקודות עצירה מותנות — לעצור רק כש-`order.id === '7c3f'` — הופכות ערמת שחת של 10,000 ריקווסטים להקפאה אחת.\n\nב-16:05, אחרי דיפלוי, הצ׳קאאוט של סינדר זרק `Cannot read properties of undefined (reading 'id')` בערך ב-8% מהסשנים. הסוכן הוסיף optional chaining בשלושה קבצים; ה-500 הפך לעגלה ריקה בשקט. נקודת עצירה על `getOwner` הראתה ש-`cart.items` היה `[]` למרות שלמשתמש היו ארבעה דברים על המסך. טאב Application: מפתח ב-`localStorage` שינה שם מ-`cart_v2` ל-`cart_v3` באותו דיפלוי, בלי מיגרציה. 40 דקות תעבורה, 220 עגלות נמחקו ברגע התשלום, ₪61,000 של GMV מתוכנן ישבו במפתח שאף אחד לא קרא. בקונסול היה ה-TypeError מהדקה הראשונה. אף אחד לא פתח אותו עד שנפח התמיכה הכריח אותם.\n\nכשסוכן רוצה לפזר `console.log` בפונקציה, תשים שם נקודת עצירה במקום ותדביק את הערכים האמיתיים לפרומפט. תשאל: מה המשתנה הזה עכשיו, בהזמנה שנכשלת? המלכודת היא optional chaining כתיקון — `cart.items?.id` גורם לקריסה להיעלם ומשאיר את באג העגלה הריקה בפרודקשן כתשובה שגויה שקטה. תשים לב גם להוראות `debugger` שהמודל משאיר, ולמטפלי שגיאות שעושים `console.error` ובולעים. סטאק אדום בקונסול הוא מתנה. דף שפשוט יושב שם הוא הסוג היקר."},
}

DETAIL_FIX.update({

# ============================== LANGS ==============================

"Typed vs untyped": {
 "en": "A type is a claim a machine checks before the program runs. `qty` is a number is not a comment — it is a proof the compiler demands on every path. Untyped code lets a value be anything and finds out at the moment the operation happens, which is usually in front of a customer. The cost of types is ceremony at write time. The cost of their absence is a class of bug that looks like arithmetic and is actually a string. Types do not make the program right; they make a family of wrong programs refuse to build.\n\nHarbor checkout took quantity from an `<input>`. Inputs are strings. JavaScript multiplied happily (`\"2\" * 10 === 20`) for seven months. A new gift-wrap branch concatenated instead: `\"2\" + 10` stored `\"210\"` as `amount_cents` after a parse that failed open. 38 invoices went out at ₪210 instead of ₪20. TypeScript would have refused the function at the form boundary. The line survived review because it read like maths. A second cousin on the same week: an agent used `userId as string` on a JSON number; 3,200 order lookups against a uuid column returned nothing, and support saw \"order not found\" for people who had just paid.\n\nFor a script you run once, skip the ceremony. For anything an agent will keep editing, types are the cheapest net you can buy — renamed fields, arguments swapped, nulls you forgot. Start in TypeScript rather than converting later. When the model silences a red squiggle with `any` or `as`, that is the finding: the checker noticed a lie. Ask what the actual type is at the boundary, and parse it (zod, or a converter) instead of asserting. An assertion is a promise the runtime will not keep.",
 "he": "טיפוס הוא טענה שמכונה בודקת לפני שהתוכנית רצה. `qty` הוא מספר זה לא הערה — זו הוכחה שהקומפיילר דורש בכל נתיב. קוד בלי טיפוסים נותן לערך להיות כל דבר ומגלה ברגע שהפעולה קורית, שזה בדרך כלל מול לקוח. מחיר הטיפוסים הוא טקס בזמן כתיבה. מחיר ההיעדר שלהם הוא משפחת באגים שנראית כמו חשבון ובעצם מחרוזת. טיפוסים לא הופכים את התוכנית לנכונה; הם גורמים למשפחה של תוכניות שגויות לסרב להיבנות.\n\nהצ׳קאאוט של הארבור לקח כמות מ-`<input>`. שדות קלט הם מחרוזות. ג׳אווהסקריפט הכפיל בשמחה (`\"2\" * 10 === 20`) שבעה חודשים. ענף עטיפת מתנה חדש שרשר במקום: `\"2\" + 10` שמר `\"210\"` כ-`amount_cents` אחרי parse שנכשל בפתוח. 38 חשבוניות יצאו ב-₪210 במקום ₪20. טייפסקריפט היה מסרב לפונקציה בגבול הטופס. השורה שרדה ריוויו כי היא נקראה כמו חשבון. בן דוד באותו שבוע: סוכן השתמש ב-`userId as string` על מספר JSON; 3,200 שליפות הזמנה מול עמודת uuid החזירו כלום, והתמיכה ראתה ״הזמנה לא נמצאה״ לאנשים שזה עתה שילמו.\n\nלסקריפט שרץ פעם אחת, דלג על הטקס. לכל דבר שסוכן ימשיך לערוך, טיפוסים הם הרשת הזולה ביותר שאפשר לקנות — שדות ששונה שמם, ארגומנטים שהוחלפו, null ששכחת. תתחיל בטייפסקריפט במקום להמיר אחר כך. כשהמודל משתיק קו אדום עם `any` או `as`, זה הממצא: הבודק שם לב לשקר. תשאל מה הטיפוס האמיתי בגבול, ותפרסר אותו (zod, או ממיר) במקום להצהיר. Assertion הוא הבטחה שהראנטיים לא יקיים."},

"Compiled vs interpreted": {
 "en": "The difference is when you find out you were wrong. A compiler reads the whole program, refuses to emit a binary if a name is missing, and you wait a minute to learn. An interpreter reads as it runs, so a broken line in the monthly path is a healthy program until the first of the month. Modern languages blur the slogan — JavaScript JIT-compiles hot paths, Java compiles twice — but the feedback loop you live in is still the thing you buy.\n\nTwo teams at the same company typo'd `sendInvioce`. The Go service failed CI in 90 seconds; it was fixed before lunch. The Python invoicing job had no build. It served traffic for eleven days and died on the 1st at 00:07 with `NameError: name 'sendInvioce' is not defined`. 14,000 invoices skipped one cycle; dunning letters went out 24 hours late; two enterprise customers opened severity-1 tickets. Same one-character mistake. The discovery cost was a customer, not a red X. A third variant: a TypeScript app with `transpileOnly: true` in the Docker start command, so `tsc` never ran. Production threw on an undefined helper that CI had never typechecked.\n\nKnow which loop your project is in, because \"it builds\" means different things. In Go, Rust, Java, C#, or `tsc --noEmit` in CI, a green build is evidence the agent's rename did not leave a dangling call. In Python or plain Node, a green start means nothing — you need tests, or a click through the path you changed. Ask the agent to add a typecheck step to CI if it is missing. The trap is treating a successful container start as proof. It is only proof that the interpreter began.",
 "he": "ההבדל הוא מתי אתה מגלה שטעית. קומפיילר קורא את כל התוכנית, מסרב לפלוט בינארי אם שם חסר, ואתה מחכה דקה כדי ללמוד. אינטרפרטר קורא תוך כדי ריצה, אז שורה שבורה בנתיב החודשי היא תוכנית בריאה עד ה-1 בחודש. שפות מודרניות מטשטשות את הסלוגן — ג׳אווהסקריפט מקמפל JIT נתיבים חמים, ג׳אווה מקמפלת פעמיים — אבל לולאת המשוב שאתה חי בה היא עדיין מה שאתה קונה.\n\nשני צוותים באותה חברה כתבו `sendInvioce`. שירות ה-Go נכשל ב-CI תוך 90 שניות; תוקן לפני הצהריים. ג׳וב החשבוניות בפייתון לא היה לו בילד. הוא שירת תעבורה אחד עשר ימים ומת ב-1 בחודש ב-00:07 עם `NameError: name 'sendInvioce' is not defined`. 14,000 חשבוניות דילגו על מחזור אחד; מכתבי דאנינג יצאו באיחור של 24 שעות; שני לקוחות אנטרפרייז פתחו כרטיסי חומרה-1. אותה טעות של תו אחד. מחיר הגילוי היה לקוח, לא איקס אדום. וריאנט שלישי: אפליקציית טייפסקריפט עם `transpileOnly: true` בפקודת העלייה בדוקר, אז `tsc` מעולם לא רץ. פרודקשן זרק על helper לא מוגדר שה-CI מעולם לא בדק טיפוסים עליו.\n\nתדע באיזו לולאה הפרויקט שלך, כי ״זה בונה״ אומר דברים שונים. ב-Go, ראסט, ג׳אווה, C#, או `tsc --noEmit` ב-CI, בילד ירוק הוא ראיה שהשינוי-שם של הסוכן לא השאיר קריאה תלויה. בפייתון או Node רגיל, עלייה ירוקה לא אומרת כלום — צריך טסטים, או קליק דרך הנתיב ששינית. תבקש מהסוכן להוסיף שלב typecheck ל-CI אם הוא חסר. המלכודת היא להתייחס לעליית קונטיינר מוצלחת כהוכחה. זו רק הוכחה שהאינטרפרטר התחיל."},

"JavaScript / TypeScript": {
 "en": "JavaScript is the language that already sits in every browser, which is why it colonised servers, CLIs and phones. That is history, not a beauty contest. TypeScript is a check at authoring time that erases itself: production runs plain JS, with no runtime cost. One language on both sides lets you share a validator — and it also lets a Date become a string at the JSON boundary while the same function still type-checks, because `as Order` is a dare, not a parser. Sharing code across the wire is a gift only if you parse at the edge; otherwise you have shared a bug that throws on one side and not the other.\n\nA shared `isPaid(order)` used `order.paidAt.getFullYear()` in the React app and in the Node worker. In the browser `paidAt` was still a Date. After `JSON.stringify` the worker received `\"2026-03-04T10:00:00.000Z\"` and threw. Overnight, 1,100 \"paid\" orders never entered the fulfilment queue; warehouse SLA missed by 16 hours. Types had been asserted at `res.json() as Order`. Zod at the boundary would have failed the parse in 2ms. Separately, optional chaining on `payment_intent.status` swallowed `incomplete`; the UI said thanks, Stripe said not captured, 64 orders, ₪18,400.\n\nStart in TypeScript even for a prototype — converting later is a project, starting is a flag. Share schemas, parse at the edge, never `as`. Watch the lockfile: a casual `npm install` on a Node repo can pull hundreds of transitive packages. Ask the agent to justify each new dependency, and remember that types disappear at runtime, so a value that crossed HTTP is untyped until you say otherwise. The trap is believing TypeScript made the JSON safe. It made the authoring safer. The wire is still strings.",
 "he": "ג׳אווהסקריפט היא השפה שכבר יושבת בכל דפדפן, ובגלל זה היא כבשה שרתים, CLIs וטלפונים. זו היסטוריה, לא תחרות יופי. טייפסקריפט היא בדיקה בזמן כתיבה שמוחקת את עצמה: פרודקשן מריץ JS רגיל, בלי עלות ריצה. שפה אחת בשני הצדדים נותנת לשתף ולידטור — וגם נותנת ל-Date להפוך למחרוזת בגבול ה-JSON בזמן שאותה פונקציה עדיין עוברת בדיקת טיפוסים, כי `as Order` הוא הימור, לא פרסר. שיתוף קוד מעל החוט הוא מתנה רק אם מפרסרים בקצה; אחרת שיתפת באג שזורק בצד אחד ולא בשני.\n\n`isPaid(order)` משותף השתמש ב-`order.paidAt.getFullYear()` באפליקציית הריאקט ובוורקר ב-Node. בדפדפן `paidAt` עדיין היה Date. אחרי `JSON.stringify` הוורקר קיבל `\"2026-03-04T10:00:00.000Z\"` וזרק. בלילה, 1,100 הזמנות ״ששולמו״ לא נכנסו לתור המימוש; SLA של המחסן פיספס ב-16 שעות. הטיפוסים הצהרו ב-`res.json() as Order`. Zod בגבול היה נכשל בפרסור ב-2ms. בנפרד, optional chaining על `payment_intent.status` בלע `incomplete`; ה-UI אמר תודה, Stripe אמר לא נלכד, 64 הזמנות, ₪18,400.\n\nתתחיל בטייפסקריפט גם לפרוטוטייפ — המרה אחר כך היא פרויקט, התחלה היא דגל. תשתף סכמות, תפרסר בקצה, לעולם לא `as`. תשמור עין על ה-lockfile: `npm install` חפיף בריפו Node יכול למשוך מאות חבילות טרנזיטיביות. תבקש מהסוכן להצדיק כל תלות חדשה, ותזכור שטיפוסים נעלמים בזמן ריצה, אז ערך שחצה HTTP הוא בלי טיפוס עד שאתה אומר אחרת. המלכודת היא להאמין שטייפסקריפט הפך את ה-JSON לבטוח. הוא הפך את הכתיבה לבטוחה יותר. החוט עדיין מחרוזות."},

"Python": {
 "en": "Python minimises the distance between an idea and a result on screen. The language is readable; the asset is the library someone already wrote for files, tables, HTTP and models. That is why it dominates glue, reports and one-off jobs. The same looseness that makes a 30-line script a gift makes a 40-file service depend on discipline the runtime will not supply: a helper that returns a dict or `None` will work until one caller forgets the check.\n\nFinance at Northline spent a day each month reconciling Stripe CSV against `orders`. A 30-line pandas script cut that to four seconds — until `requirements.txt` listed `pandas` with no pin. CI in March got 2.0.3. A fresh image in September got 2.2.2, where `groupby(...).apply` changed enough that the join produced 0 rows. The monthly report emailed a blank table at 07:00. Six hours of \"the warehouse is empty\" later, someone ran `pip freeze` on the old laptop. Pinning `pandas==2.2.2` and a one-row smoke test on the report job would have failed CI in 20 seconds instead of failing finance in six hours.\n\nPick Python when the work is process this file, call that API, analyse these rows. Then add what the language will not: type hints plus mypy or pyright, a lock file, a virtualenv per project. Agents skip all three unless you demand them. Ask: where is the venv, and will this pip install the same versions in a year? The trap is a global Python on the laptop fighting every project, and an unpinned requirements file that turns \"works on my machine\" into a calendar event. Also watch `except Exception: pass` in generated scripts — the nightly job that writes empty rows instead of dying.",
 "he": "פייתון ממזער את המרחק בין רעיון לתוצאה על המסך. השפה קריאה; הנכס הוא הספרייה שמישהו כבר כתב בשביל קבצים, טבלאות, HTTP ומודלים. בגלל זה היא שולטת בגלו, בדוחות ובעבודות חד-פעמיות. אותה רפיון שהופכת סקריפט של 30 שורות למתנה הופכת שירות של 40 קבצים לתלוי במשמעת שהראנטיים לא יספק: helper שמחזיר dict או `None` יעבוד עד שקורא אחד ישכח לבדוק.\n\nהכספים בנורת׳ליין בילו יום בחודש בהתאמת CSV של Stripe מול `orders`. סקריפט pandas של 30 שורות קיצר את זה לארבע שניות — עד ש-`requirements.txt` רשם `pandas` בלי נעיצה. CI במרץ קיבל 2.0.3. אימג׳ טרי בספטמבר קיבל 2.2.2, שם `groupby(...).apply` השתנה מספיק כדי שהג׳וין ייצר 0 שורות. הדוח החודשי מייל טבלה ריקה ב-07:00. אחרי שש שעות של ״המחסן ריק״, מישהו הריץ `pip freeze` על הלפטופ הישן. נעיצת `pandas==2.2.2` וטסט עשן של שורה אחת על ג׳וב הדוח היו מפילים את ה-CI ב-20 שניות במקום להפיל את הכספים בשש שעות.\n\nתבחר פייתון כשהעבודה היא לעבד את הקובץ הזה, לקרוא ל-API ההוא, לנתח את השורות האלה. ואז תוסיף מה שהשפה לא תוסיף: רמזי טיפוסים ועוד mypy או pyright, קובץ lock, וירטואל-env לכל פרויקט. סוכנים מדלגים על שלושתם אם לא דורשים. תשאל: איפה ה-venv, והאם ה-pip הזה יתקין את אותן גרסאות בעוד שנה? המלכודת היא פייתון גלובלי על הלפטופ שנלחם בכל פרויקט, וקובץ requirements לא נעוץ שהופך ״אצלי זה עובד״ לאירוע ביומן. תשים לב גם ל-`except Exception: pass` בסקריפטים שנוצרים — הג׳וב הלילי שכותב שורות ריקות במקום למות."},

"Go / Rust / Java / C#": {
 "en": "These languages charge ceremony up front because at some scale a mistake costs more than the ceremony. Go buys small, boring services and a single binary. Rust buys memory safety without a garbage collector, paid in compiler fights. Java and C# buy decades of libraries, ops knowledge, and frameworks that already decided your folder layout. You rarely pick them from a blog post about speed. You pick them because the team, the employer, or the ecosystem already speaks them. Speed is a measurement you take after the product exists, not a reason to start in a language nobody on-call can read.\n\nThumbly needed image resizing. A fan of Rust spent three weeks on a service that did 4ms per thumbnail at 60MB RSS. Traffic was eleven images a minute. A colleague's Node stopgap, four hours of work, used 400MB and was fine. Later, a Java shop rewrote checkout in Go \"for performance\": nine weeks, two engineers, p95 from 180ms to 140ms. The missed feature was Apple Pay, which a competitor shipped in week three. Rust was the better program. Go was the better binary. Neither was the better quarter.\n\nFor AI-assisted work, treat these names as a reading guide more than a shopping list. A `go.mod` means explicit `if err != nil` on every line — an agent that swallows errors is wrong for that repo. A `pom.xml` or `.csproj` means conventions you follow, not a tutorial the model remembers. Ask: will this service be deployed as one binary, and does the team already run this stack at 3am? The trap is an agent scaffolding Actix or Spring for a weekend prototype you will throw away, or ignoring the error style of the language you are actually in. Choose boring, choose spoken, measure before you switch for speed.",
 "he": "השפות האלה גובות טקס מראש כי בסקייל מסוים טעות עולה יותר מהטקס. Go קונה שירותים קטנים ומשעממים ובינארי אחד. ראסט קונה בטיחות זיכרון בלי GC, ומשלמים על זה במאבקים עם הקומפיילר. ג׳אווה ו-C# קונות עשורים של ספריות, ידע תפעול, ופריימוורקים שכבר החליטו על מבנה התיקיות. לעיתים נדירות בוחרים אותן מפוסט בבלוג על מהירות. בוחרים אותן כי הצוות, המעסיק או האקוסיסטם כבר מדברים אותן. מהירות היא מדידה שעושים אחרי שהמוצר קיים, לא סיבה להתחיל בשפה שאף אחד בכוננות לא יודע לקרוא.\n\nת׳מבלי היו צריכים שינוי גודל תמונות. חובב ראסט בילה שלושה שבועות על שירות שעשה 4ms לת׳אמבנייל ב-60MB RSS. התעבורה הייתה אחת עשרה תמונות בדקה. פתרון הביניים ב-Node של עמית, ארבע שעות עבודה, השתמש ב-400MB והיה בסדר. אחר כך, חברת ג׳אווה שכתבה צ׳קאאוט ב-Go ״בשביל ביצועים״: תשעה שבועות, שני מהנדסים, p95 מ-180ms ל-140ms. הפיצ׳ר שהוחמץ היה Apple Pay, שמתחרה שחרר בשבוע השלישי. ראסט הייתה התוכנית הטובה יותר. Go היה הבינארי הטוב יותר. אף אחד מהם לא היה הרבעון הטוב יותר.\n\nלעבודה עם AI, תתייחס לשמות האלה כמדריך קריאה יותר מאשר רשימת קניות. `go.mod` אומר `if err != nil` מפורש בכל שורה — סוכן שבולע שגיאות שגוי לריפו הזה. `pom.xml` או `.csproj` אומרים מוסכמות שעוקבים אחריהן, לא טוטוריאל שהמודל זוכר. תשאל: האם השירות הזה יעלה כבינארי אחד, והאם הצוות כבר מריץ את הסטאק הזה ב-03:00? המלכודת היא סוכן שמסקאפל Actix או Spring לפרוטוטייפ של סוף שבוע שתזרוק, או שמתעלם מסגנון השגיאות של השפה שאתה באמת בה. תבחר משעמם, תבחר מדובר, תמדוד לפני שאתה מחליף בשביל מהירות."},

"Package manager": {
 "en": "A package manager makes the same tree of code appear on two machines: your laptop, CI, production. You declare a range. The tool solves a graph. The lock file writes down exactly what it solved. Without that file, a build is a function of what the registry felt like that morning. With it, a build in a year can be the same bytes. That is the whole product. The range in `package.json` is a wish. The lock file is history.\n\nCartel had `\"stripe\": \"^12.1.0\"` and no habit of `npm ci`. A developer laptop, installed in March, had 12.1.0. CI in June resolved 12.9.0, which changed a default from automatic payment methods to explicit ones. Unit tests mocked Stripe and stayed green. Production declined 19% of cards for 70 minutes after the deploy. Nobody had changed application code. The caret had. Switching CI to `npm ci` made the next drift a loud mismatch instead of a silent new library. A cousin incident: an agent ran `npm install axios` and the lockfile diff was 1,840 lines. Three packages deep sat a deprecated `follow-redirects` release with a published advisory.\n\nCommit the lock file — `package-lock.json`, `pnpm-lock.yaml`, `poetry.lock`, `Cargo.lock`, `go.sum` — always. When an agent adds a dependency, read the lock diff, not only the one-line manifest change. Ask CI to install from the lock (`npm ci`, `pnpm i --frozen-lockfile`). The trap is `npm install` in CI, which updates the lock, and the other trap is not committing the lock \"because it is generated\". Generated is the point. Also refuse `*` and `latest` in ranges. Yesterday's green build is not a promise about tomorrow's registry.",
 "he": "מנהל חבילות גורם לאותו עץ קוד להופיע על שתי מכונות: הלפטופ שלך, CI, פרודקשן. אתה מצהיר על טווח. הכלי פותר גרף. קובץ ה-lock רושם בדיוק מה הוא פתר. בלי הקובץ הזה, בילד הוא פונקציה של מה שהרג׳יסטרי הרגיש באותו בוקר. איתו, בילד בעוד שנה יכול להיות אותם בתים. זה כל המוצר. הטווח ב-`package.json` הוא משאלה. קובץ ה-lock הוא היסטוריה.\n\nקרטל היה להם `\"stripe\": \"^12.1.0\"` ובלי הרגל של `npm ci`. לפטופ מפתח, הותקן במרץ, היה 12.1.0. CI ביוני פתר 12.9.0, ששינה ברירת מחדל מאמצעי תשלום אוטומטיים למפורשים. יוניט טסטים עשו mock ל-Stripe ונשארו ירוקים. פרודקשן דחה 19% מהכרטיסים במשך 70 דקות אחרי הדיפלוי. אף אחד לא שינה קוד אפליקציה. הגגון שינה. מעבר של CI ל-`npm ci` הפך את הסחיפה הבאה לאי-התאמה רועשת במקום לספרייה חדשה שקטה. תקלה בת דודה: סוכן הריץ `npm install axios` והדיף של ה-lockfile היה 1,840 שורות. שלוש חבילות פנימה ישב ריליס deprecated של `follow-redirects` עם התרעה שפורסמה.\n\nתעשה קומיט לקובץ ה-lock — `package-lock.json`, `pnpm-lock.yaml`, `poetry.lock`, `Cargo.lock`, `go.sum` — תמיד. כשסוכן מוסיף תלות, תקרא את דיף ה-lock, לא רק את שינוי המניפסט של שורה אחת. תבקש מה-CI להתקין מה-lock (`npm ci`, `pnpm i --frozen-lockfile`). המלכודת היא `npm install` ב-CI, שמעדכן את ה-lock, והמלכודת השנייה היא לא לעשות קומיט ל-lock ״כי הוא מחולל״. מחולל זו הנקודה. תסרב גם ל-`*` ול-`latest` בטווחים. בילד ירוק מאתמול אינו הבטחה לגבי הרג׳יסטרי של מחר."},

"Dependency": {
 "en": "A dependency is a stranger you hire forever. You take their bugs, their advisories, their breaking majors, their model of the problem, and their right to stop answering email. Day one feels free because `npm install` is one line. Day four hundred is a prototype-pollution notice four levels down that forces a framework bump, a Node bump, and a Docker rebuild you did not budget. The question is never can this library do it. It is would I rather own twenty lines or own this relationship.\n\nA Next.js shop had 14 direct dependencies and 1,190 packages in `node_modules`. An advisory landed on a helper four hops in. `npm audit fix --force` jumped a plugin major, which needed Node 20, which broke the Node 18 base image. Two engineers, two days. In the same repo, `is-email-valid` (40KB, last publish 2021) checked emails. A regex of three lines would have done it with no advisory feed. Meanwhile an agent, asked to format dates, added `moment` next to existing `date-fns` and `dayjs` — three clocks, two of them unmaintained, one extra 300KB in the checkout bundle, p75 on mobile up 180ms.\n\nWhen a new name appears in a diff, ask what it does, how many packages it brought, and when it last released. Four years of silence is abandoned, not stable. Own crypto, time zones, and protocol parsers as libraries; own a one-function helper as code. Agents install by default because an import is the shortest answer. The trap is a hallucinated package name that happens to exist on the registry under new ownership, and the cousin trap: three libraries for one job. Delete before you add. `npm ls --all | wc -l` is a number you should see once per PR that touches `package.json`.",
 "he": "תלות היא זר שאתה שוכר לנצח. אתה לוקח את הבאגים שלו, את ההתרעות שלו, את המייג׳ורים השוברים, את המודל שלו לבעיה, ואת הזכות שלו להפסיק לענות למייל. יום אחד מרגיש חינם כי `npm install` הוא שורה אחת. יום ארבע מאות הוא התרעת prototype-pollution ארבע רמות פנימה שמכריחה קפיצת פריימוורק, קפיצת Node, ובילד דוקר שלא תקצבת. השאלה היא אף פעם לא האם הספרייה יודעת. היא האם אני מעדיף להחזיק עשרים שורות או להחזיק את מערכת היחסים הזאת.\n\nחנות Next.js היו לה 14 תלויות ישירות ו-1,190 חבילות ב-`node_modules`. התרעה נחתה על helper ארבע קפיצות פנימה. `npm audit fix --force` קפץ מייג׳ר של פלאגין, שדרש Node 20, ששבר את אימג׳ הבסיס של Node 18. שני מהנדסים, יומיים. באותו ריפו, `is-email-valid` (40KB, פרסום אחרון 2021) בדק אימיילים. רג׳קס של שלוש שורות היה עושה את זה בלי פיד התרעות. בינתיים סוכן, שהתבקש לפרמט תאריכים, הוסיף `moment` ליד `date-fns` ו-`dayjs` קיימים — שלושה שעונים, שניים מהם לא מתוחזקים, 300KB נוספים בבאנדל הצ׳קאאוט, p75 במובייל עלה ב-180ms.\n\nכששם חדש מופיע בדיף, תשאל מה הוא עושה, כמה חבילות הוא הביא, ומתי הוא שוחרר לאחרונה. ארבע שנים של שקט זה נטוש, לא יציב. תחזיק קריפטו, אזורי זמן ופרסרי פרוטוקול כספריות; תחזיק helper של פונקציה אחת כקוד. סוכנים מתקינים כברירת מחדל כי ייבוא הוא התשובה הקצרה ביותר. המלכודת היא שם חבילה מומצא שבמקרה קיים ברג׳יסטרי תחת בעלות חדשה, ומלכודת בת דודה: שלוש ספריות לעבודה אחת. תמחק לפני שאתה מוסיף. `npm ls --all | wc -l` הוא מספר שכדאי לראות פעם בכל PR שנוגע ב-`package.json`."},

"Framework vs library": {
 "en": "You call a library; a framework calls you. A library is a drawer: `format(date)` when you want it. A framework is a building: this folder is a route, this filename is a loading state, sessions live here. Leverage is enormous when its opinions match your problem. The tax is permanent when they do not — you will spend weeks fighting a room layout you agreed to on day one, in an afternoon, because the tutorial was pretty.\n\nAn internal tool took Next.js and had auth, routing and a deploy in one afternoon. Nine months later they needed login via a customer's IdP, returning to a per-tenant subdomain. The framework's session cookie was bound to one host. Two weeks of middleware later, the next framework upgrade broke the workaround. The same feature on Express plus a routing library was estimated at two days — days they would have paid on every feature before that. A smaller version of the same tax: an agent mixed `pages/` and `app/` routers in one repo. After deploy, 40% of URLs 404'd. The convention was the product, and nobody had picked one convention.\n\nEvaluate a framework by a three-year-old app larger than yours, not by the getting-started. For agents, popularity is a feature: the conventional answer is in the training data. A mainstream framework you mildly dislike will often generate better code than a unique snowflake layout. Ask which of your requirements sit outside its opinions before you commit — those are the weeks. The trap is two frameworks in one repo, or a library used as if it were a framework (invented folder magic nobody else knows). Pick one building, or pick drawers. Do not build a shed inside someone else's kitchen.",
 "he": "אתה קורא לספרייה; פריימוורק קורא לך. ספרייה היא מגירה: `format(date)` כשאתה רוצה. פריימוורק הוא בניין: התיקייה הזאת היא ראוט, שם הקובץ הזה הוא מצב טעינה, סשנים חיים כאן. המינוף עצום כשהדעות שלו מתאימות לבעיה. המס קבוע כשלא — תבלה שבועות בלהילחם בפריסת חדרים שהסכמת לה ביום הראשון, בצהריים, כי הטוטוריאל היה יפה.\n\nכלי פנימי לקח Next.js וקיבל הזדהות, ניתוב ודיפלוי בצהריים אחד. תשעה חודשים אחר כך הם נזקקו להתחברות דרך IdP של לקוח, עם חזרה לסאב-דומיין לכל טננט. קוקי הסשן של הפריימוורק היה קשור למארח אחד. אחרי שבועיים של מידלוור, שדרוג הפריימוורק הבא שבר את הפתרון הזמני. אותו פיצ׳ר על Express ועוד ספריית ניתוב הוערך ביומיים — ימים שהיו משלמים בכל פיצ׳ר לפני כן. גרסה קטנה יותר של אותו מס: סוכן ערבב ראוטרים של `pages/` ו-`app/` בריפו אחד. אחרי דיפלוי, 40% מה-URLs החזירו 404. המוסכמה הייתה המוצר, ואף אחד לא בחר מוסכמה אחת.\n\nתעריך פריימוורק לפי אפליקציה בת שלוש שנים שגדולה משלך, לא לפי ההתחלה. לסוכנים, פופולריות היא פיצ׳ר: התשובה המוסכמת נמצאת בדאטה האימון. פריימוורק מיינסטרים שאתה לא אוהב במיוחד ייצר לעיתים קוד טוב יותר מפריסת פתית שלג ייחודית. תשאל אילו מהדרישות שלך יושבות מחוץ לדעות שלו לפני שאתה מתחייב — אלה השבועות. המלכודת היא שני פריימוורקים בריפו אחד, או ספרייה בשימוש כאילו היא פריימוורק (קסם תיקיות מומצא שאף אחד אחר לא מכיר). תבחר בניין אחד, או מגירות. אל תבנה סככה בתוך המטבח של מישהו אחר."},

"Semantic versioning": {
 "en": "Semantic versioning is a promise written as three numbers: major.minor.patch — breaks, adds, fixes. A caret `^1.4.2` allows minors and patches below 2.0.0. A tilde `~1.4.2` allows patches below 1.5.0. An exact `1.4.2` allows that version. Nothing in the registry enforces the promise. People release breaking changes as patches. Tools still trust the numbers, which is why the lock file is the adult in the room and the range is a hint to future-you.\n\nOn a Tuesday, an agent changed `\"zod\": \"3.22.4\"` to `\"zod\": \"^4.0.0\"` while \"updating deps\". Zod 4 rewrote the parser API. CI installed 4.1.5. 14 schema files failed at runtime, not at typecheck, because the old types were still imported from a path that existed and behaved differently. Checkout 500'd for 47 minutes, 220 failed pays, roughly ₪54,000 of GMV retried by users who bothered. `npm audit` was clean. The major was the point of the major. A milder version: `\"express\": \"*\"` in a toy repo copied to production, and a new Express major on a Friday registry publish took down staging Saturday morning.\n\nRead every range an agent writes. Ask: did you mean a major bump, and have you read the changelog? Models suggest versions that were common in training data — old, sometimes advisory-laden. Check npm for latest and `npm audit` before you merge. The trap is `^` on a library whose majors are frequent (UI kits, Stripe SDKs, ORMs), and the other trap is pinning forever so you never get patches. Default to `^` plus a lockfile plus `npm ci`. When you bump a major, that is the PR — not a side effect of \"refresh packages\".",
 "he": "ניהול גרסאות סמנטי הוא הבטחה כתובה כשלושה מספרים: major.minor.patch — שובר, מוסיף, מתקן. גגון `^1.4.2` מתיר מינורים ופאצ׳ים מתחת ל-2.0.0. טילדה `~1.4.2` מתירה פאצ׳ים מתחת ל-1.5.0. מדויק `1.4.2` מתיר את הגרסה הזאת. שום דבר ברג׳יסטרי לא אוכף את ההבטחה. אנשים משחררים שינויים שוברים כפאצ׳ים. כלים עדיין סומכים על המספרים, ובגלל זה קובץ ה-lock הוא המבוגר בחדר והטווח הוא רמז לעצמך בעתיד.\n\nביום שלישי, סוכן שינה `\"zod\": \"3.22.4\"` ל-`\"zod\": \"^4.0.0\"` בזמן ״עדכון תלויות״. Zod 4 שכתב את API הפרסר. CI התקין 4.1.5. 14 קבצי סכמה נכשלו בזמן ריצה, לא בבדיקת טיפוסים, כי הטיפוסים הישנים עדיין יובאו מנתיב שהתקיים והתנהג אחרת. הצ׳קאאוט החזיר 500 במשך 47 דקות, 220 תשלומים נכשלים, בערך ₪54,000 GMV שמשתמשים טרחו לנסות שוב. `npm audit` היה נקי. המייג׳ר היה כל הנקודה של המייג׳ר. גרסה עדינה יותר: `\"express\": \"*\"` בריפו צעצוע שהועתק לפרודקשן, ומייג׳ר חדש של Express בפרסום רג׳יסטרי בשישי הפיל סטייג׳ינג בשבת בבוקר.\n\nתקרא כל טווח שסוכן כותב. תשאל: התכוונת לקפיצת מייג׳ר, וקראת את הצ׳ינג׳לוג? מודלים מציעים גרסאות שהיו נפוצות בדאטה האימון — ישנות, לפעמים עמוסות התרעות. תבדוק ב-npm מה העדכני ו-`npm audit` לפני מיזוג. המלכודת היא `^` על ספרייה שהמייג׳ורים שלה תכופים (קיטי UI, SDKs של Stripe, ORMs), והמלכודת השנייה היא לנעוץ לנצח כדי שלעולם לא תקבלו פאצ׳ים. ברירת מחדל: `^` ועוד lockfile ועוד `npm ci`. כשמקפיצים מייג׳ר, זה ה-PR — לא תופעת לוואי של ״רענון חבילות״."},
})

DETAIL_FIX.update({

# ============================== TESTING ==============================

"Test": {
 "en": "A test is an executable claim: given this setup, when this happens, this value must be that. It does not prove the program is correct. It proves that one behaviour you cared about has not silently changed. That is a change-safety tool. The useful question is not does this run, it is would this have caught the mistake I am actually afraid of — the double charge, the other user's order, the empty cart that still returns 200. A test you never run is documentation. A test CI runs on every push is a door that does not open for a broken change.\n\nAn agent was asked to \"fix checkout 500 on empty cart\". It did: the handler returned 200 and `{id: null, items: []}`. No test existed. Over a weekend, 31 phantom orders hit the warehouse as pick-lists with no SKU; three pickers spent Monday morning walking aisles. A one-liner `expect(res.status).toBe(400)` would have failed the PR. After the incident they added the test, then found the agent had also stopped returning 404 for unknown SKUs — same pattern, 14 support tickets, no crash. Green deploys, wrong behaviour, the test suite still at twelve files none of which touched `/checkout`.\n\nWhen an agent writes a feature, demand the test that would have failed before the feature existed. Paste the assertion you care about in the prompt. The trap is a suite that starts the server and asserts that `typeof res.body === 'object'` — it will pass while checkout creates garbage. The other trap is skipping tests because \"it worked in the browser once\". Browsers do not run at 3am on someone else's machine after a dependency bump. If you only write three tests in a product, make them ownership, money, and deletion.",
 "he": "טסט הוא טענה ברת-הרצה: בהינתן ההכנה הזאת, כשזה קורה, הערך הזה חייב להיות ההוא. הוא לא מוכיח שהתוכנית נכונה. הוא מוכיח שהתנהגות אחת שאכפת לך ממנה לא השתנתה בשקט. זה כלי בטיחות לשינויים. השאלה השימושית היא לא האם זה רץ, אלא האם זה היה תופס את הטעות שאני באמת מפחד ממנה — החיוב הכפול, ההזמנה של המשתמש האחר, העגלה הריקה שעדיין מחזירה 200. טסט שמעולם לא רץ הוא תיעוד. טסט שה-CI מריץ בכל פוש הוא דלת שלא נפתחת לשינוי שבור.\n\nסוכן התבקש ״לתקן 500 בצ׳קאאוט על עגלה ריקה״. הוא תיקן: המטפל החזיר 200 ו-`{id: null, items: []}`. לא היה טסט. בסוף שבוע, 31 הזמנות פנטום הגיעו למחסן כרשימות ליקוט בלי SKU; שלושה מלגזנים בילו את בוקר יום שני במעברים. שורת `expect(res.status).toBe(400)` הייתה מפילה את ה-PR. אחרי התקלה הם הוסיפו את הטסט, ואז גילו שהסוכן גם הפסיק להחזיר 404 ל-SKU לא מוכר — אותו דפוס, 14 פניות תמיכה, בלי קריסה. דיפלויים ירוקים, התנהגות שגויה, סוויטת הטסטים עדיין שניים עשר קבצים שאף אחד מהם לא נגע ב-`/checkout`.\n\nכשסוכן כותב פיצ׳ר, תדרוש את הטסט שהיה נכשל לפני שהפיצ׳ר היה קיים. תדביק בפרומפט את ה-assertion שאכפת לך ממנה. המלכודת היא סוויטה שמדליקה שרת וטוענת ש-`typeof res.body === 'object'` — היא תעבור בזמן שהצ׳קאאוט יוצר זבל. המלכודת השנייה היא לדלג על טסטים כי ״זה עבד בדפדפן פעם אחת״. דפדפנים לא רצים ב-03:00 על מכונה של מישהו אחר אחרי קפיצת תלות. אם אתה כותב רק שלושה טסטים במוצר, שיהיו בעלות, כסף, ומחיקה."},

"Assertion": {
 "en": "The assertion is the meaning. Setup, fixtures, helpers — all of that exists to make one line possible: this equals that, or the test is red. A test with no assertion is a smoke that the process did not explode. It will pass while the logic is wrong, and it will pass forever, which is worse than no test because it looks like coverage. Assert the thing you would shout about in Slack at 2am, not the type of the response. If the assertion cannot go red on the bug you fear, it is decoration around a green tick.\n\nA well-meaning agent added `orders.test.ts` with 40 cases. 39 of them ended `expect(res.status).toBe(200)`. The 40th created user A, created an order, then fetched it as user B and expected 200 too. IDOR, green suite. An intern on week two, following the same test as documentation, built a support tool that pulled 14,000 other merchants' invoices into a CSV in 90 seconds. Finance noticed on the monthly close. The missing line was `expect(res.status).toBe(403)`. Everything around it — factories, logins, `beforeEach` — had been carefully generated.\n\nWhen you review agent tests, jump to the `expect`. If it checks status 200, length > 0, or \"not to throw\", ask it to assert the business fact: the amount in cents, the owner id, the rejected coupon, the row count of 0 after delete. The trap is `assert.ok(true)` leftovers, snapshots of entire JSON blobs that churn on every timestamp, and matchers so loose they cannot fail. One sharp assertion beats ten polite ones. If adding an assertion would make today's code red, that is the test you needed yesterday.",
 "he": "ה-assertion הוא המשמעות. הכנה, פיקסצ׳רים, הלפרים — כל זה קיים כדי לאפשר שורה אחת: זה שווה לזה, או שהטסט אדום. טסט בלי assertion הוא עשן שהתהליך לא התפוצץ. הוא יעבור בזמן שהלוגיקה שגויה, והוא יעבור לנצח, וזה גרוע יותר מאין טסט כי זה נראה כמו כיסוי. תטען את הדבר שהיית צועק עליו בסלאק ב-02:00, לא את טיפוס התשובה. אם ה-assertion לא יכול להאדים על הבאג שאתה מפחד ממנו, הוא קישוט סביב וי ירוק.\n\nסוכן בכוונות טובות הוסיף `orders.test.ts` עם 40 מקרים. 39 מהם נגמרו ב-`expect(res.status).toBe(200)`. ה-40 יצר משתמש A, יצר הזמנה, ואז שלף אותה כמשתמש B וציפה גם כן ל-200. IDOR, סוויטה ירוקה. מתמחה בשבוע השני, בעקבות אותו טסט כתיעוד, בנה כלי תמיכה שמשך 14,000 חשבוניות של סוחרים אחרים ל-CSV ב-90 שניות. הכספים שמו לב בסגירה החודשית. השורה החסרה הייתה `expect(res.status).toBe(403)`. כל מה מסביב — פקטוריז, לוגין, `beforeEach` — חולל בקפידה.\n\nכשאתה סוקר טסטים של סוכן, תקפוץ ל-`expect`. אם הוא בודק סטטוס 200, אורך > 0, או ״לא לזרוק״, תבקש ממנו לטעון את העובדה העסקית: הסכום בסנטים, מזהה הבעלים, הקופון שנדחה, ספירת שורות 0 אחרי מחיקה. המלכודת היא שאריות `assert.ok(true)`, סנאפשוטים של בלוב JSON שלם שזז בכל חותמת זמן, ו-matchers כל כך רפויים שהם לא יכולים להיכשל. assertion חד אחד מנצח עשרה מנומסים. אם הוספת assertion תהפוך את הקוד של היום לאדום, זה הטסט שהיית צריך אתמול."},

"Unit / Integration / E2E": {
 "en": "Three distances from reality. A unit test calls one function with doubles for everything else — fast, precise, blind to the database. An integration test runs several real pieces together, usually with a real Postgres. An E2E test drives a browser like a person. Each step down is slower and tells you less about which line is wrong, and more about whether the product actually works. You want many of the first, some of the second, and a few of the third on the paths that take money or delete data.\n\nMailcart had 400 unit tests, all green, 22 seconds. None opened Postgres. The unique constraint on `users.email` existed in a TypeScript type and not in the schema. After three weeks of ads, 2,100 duplicate emails sat in the table — the ORM insert succeeded, the second row just… existed. Password reset emailed a random twin. An integration test on `POST /signup` against testcontainers Postgres would have failed on the second insert in 800ms. They also had zero E2E tests; a CSS change hid the Pay button for Safari 16 only, noticed after ₪12,000 of abandoned carts on iPhones.\n\nAsk an agent for the layer that matches the risk: a pure function of tax → unit; a query plus a constraint → integration; checkout in a browser → one E2E. The trap is 400 units and a production database that disagrees, or a 40-minute Playwright suite nobody runs. Another trap: E2E that reimplements business rules in the test instead of observing them. Keep E2E short and boring: login, add to cart, pay, see a confirmation id. When the unit suite is green and production is not, you are missing the middle layer.",
 "he": "שלושה מרחקים מהמציאות. יוניט טסט קורא לפונקציה אחת עם כפילים לכל השאר — מהיר, מדויק, עיוור לדאטהבייס. טסט אינטגרציה מריץ כמה חלקים אמיתיים יחד, בדרך כלל עם פוסטגרס אמיתי. טסט E2E מנהג דפדפן כמו אדם. כל צעד למטה איטי יותר ואומר פחות איזו שורה שגויה, ויותר האם המוצר באמת עובד. רוצים הרבה מהראשונים, כמה מהשניים, ומעט מהשלישי על הנתיבים שלוקחים כסף או מוחקים דאטה.\n\nמיילקארט היו להם 400 יוניט טסטים, כולם ירוקים, 22 שניות. אף אחד לא פתח פוסטגרס. אילוץ הייחודיות על `users.email` התקיים בטיפוס טייפסקריפט ולא בסכמה. אחרי שלושה שבועות של מודעות, 2,100 אימיילים כפולים ישבו בטבלה — ה-insert של ה-ORM הצליח, השורה השנייה פשוט… הייתה. איפוס סיסמה מייל לתאום אקראי. טסט אינטגרציה על `POST /signup` מול פוסטגרס ב-testcontainers היה נכשל באינסרט השני ב-800ms. גם לא היו להם טסטי E2E; שינוי CSS הסתיר את כפתור שלם ב-Safari 16 בלבד, התגלה אחרי ₪12,000 של עגלות נטושות באייפונים.\n\nתבקש מסוכן את השכבה שמתאימה לסיכון: פונקציה טהורה של מס → יוניט; שאילתה ועוד אילוץ → אינטגרציה; צ׳קאאוט בדפדפן → E2E אחד. המלכודת היא 400 יוניטים ודאטהבייס פרודקשן שלא מסכים, או סוויטת Playwright של 40 דקות שאף אחד לא מריץ. מלכודת נוספת: E2E שמממש מחדש חוקים עסקיים בטסט במקום לצפות בהם. תשמור E2E קצר ומשעמם: לוגין, הוספה לעגלה, תשלום, לראות מזהה אישור. כשסוויטת היוניט ירוקה ופרודקשן לא, חסרה לך שכבת האמצע."},

"Mock / Fixture": {
 "en": "A mock is a fake you control: Stripe always returns `ch_1`, email always succeeds. A fixture is known data the test starts from: a user, an order, a coupon. Mocks save you from networks you do not own. They also freeze an assumption. When Stripe's real signature header changes, the mock still nods, and production is the first place that meets the new truth. Fixtures go stale too — a status enum the seed never got. Mock what you cannot run. Run what you own, especially the database. A fake that always succeeds is how a suite stays green while Stripe, in the real world, does not.\n\nPaylane's suite mocked `stripe.charges.create` to `{id:'ch_1', paid:true}` in every test. 96% coverage on `payments.ts`. In production the webhook handler never verified `Stripe-Signature`. An attacker was not required: Stripe's own retry, unsigned in a staging misconfig that leaked to prod via an env copy, marked 180 orders paid. Stripe's dashboard showed 0 captured. The hole was ₪67,000 and 48 hours. A fixture of a real signed payload from Stripe's CLI, plus a test that unsigned bodies return 400, would have failed CI. They had mocked the thing they needed to get right.\n\nWhen an agent mocks `./db`, stop. Ask it to use a real Postgres in CI (service container, testcontainers) and to mock only Stripe, mail, and the freight API. Look at fixtures for money: amounts in cents, statuses that exist. The trap is a mock that returns the happy shape your code already assumes, so the test cannot see a missing field. The other trap is sharing one database across tests without isolation — fixtures leak, order depends on luck. If the mock is the system under test, you are testing the mock.",
 "he": "מוק הוא זיוף שאתה שולט בו: Stripe תמיד מחזיר `ch_1`, מייל תמיד מצליח. פיקסצ׳ר הוא דאטה ידוע שהטסט מתחיל ממנו: משתמש, הזמנה, קופון. מוקים חוסכים ממך רשתות שאינן שלך. הם גם מקפיאים הנחה. כשכותרת החתימה האמיתית של Stripe משתנה, המוק עדיין מהנהן, ופרודקשן הוא המקום הראשון שפוגש את האמת החדשה. גם פיקסצ׳רים מתיישנים — enum של סטטוס שהסיד מעולם לא קיבל. תזייף מה שאי אפשר להריץ. תריץ מה שבבעלותך, במיוחד את הדאטהבייס. זיוף שתמיד מצליח הוא איך שסוויטה נשארת ירוקה בזמן ש-Stripe, בעולם האמיתי, לא.\n\nהסוויטה של פיילן עשתה mock ל-`stripe.charges.create` ל-`{id:'ch_1', paid:true}` בכל טסט. 96% כיסוי על `payments.ts`. בפרודקשן מטפל ה-webhook מעולם לא אימת `Stripe-Signature`. לא היה צריך תוקף: הניסיון החוזר של Stripe עצמו, בלי חתימה במיסקונפיג של סטייג׳ שדלף לפרוד דרך העתקת env, סימן 180 הזמנות כששולמו. הדשבורד של Stripe הראה 0 נלכדים. החור היה ₪67,000 ו-48 שעות. פיקסצ׳ר של פיילוד חתום אמיתי מ-CLI של Stripe, ועוד טסט שגופים לא חתומים מחזירים 400, היה מפיל את ה-CI. הם זייפו את הדבר שהיו צריכים להביא נכון.\n\nכשסוכן עושה mock ל-`./db`, תעצור. תבקש ממנו פוסטגרס אמיתי ב-CI (קונטיינר שירות, testcontainers) ולזייף רק Stripe, מייל, ו-API השילוח. תסתכל על פיקסצ׳רים לכסף: סכומים בסנטים, סטטוסים שקיימים. המלכודת היא מוק שמחזיר את צורת השמחה שהקוד שלך כבר מניח, כך שהטסט לא יכול לראות שדה חסר. המלכודת השנייה היא שיתוף דאטהבייס אחד בין טסטים בלי בידוד — פיקסצ׳רים דולפים, הסדר תלוי במזל. אם המוק הוא המערכת תחת הבדיקה, אתה בודק את המוק."},

"Regression": {
 "en": "A regression is a bug with a past: it worked, then it did not. The highest-value test you will write is the one you add in the same hour you fix it, because it is pinned to a mistake that already happened in your system, not to a generic spec. Bugs cluster in the same misunderstandings — time zones, money, permissions — so the suite that grows from real incidents becomes a map of your particular cliffs. A generic test written from a spec will not catch the DST bug you have already shipped twice; only a test named after that weekend will.\n\nTwice in one year, Oak's order timestamps shifted a day for UK customers around DST. First fix: `moment(date).utc()`. Second fix, after an agent \"cleaned up dates\": `new Date(row.created)`. On the last Sunday of March, 890 orders moved to yesterday in the ops board; 40 warehouse picks went to the wrong wave; same-day courier fees of ₪9,400 were spent on packages that were not late until the dashboard said they were. There was no test named `dst-uk-order-stays-on-the-day`. There were 200 tests for the React header.\n\nWhen a bug is fixed, the next message to the agent is: write a failing test that reproduces this, then the fix. Keep the test. Name it after the incident. The trap is fixing without pinning, then letting the next refactor — often by the same model — revive it. The other trap is a huge \"regression suite\" that is just the unit suite renamed, with no link to a past failure. If you cannot point at a ticket, a date, and a number, it is not a regression test yet. It is hope.",
 "he": "רגרסיה היא באג עם עבר: זה עבד, ואז זה לא. הטסט בעל הערך הגבוה ביותר שתכתוב הוא זה שאתה מוסיף באותה שעה שאתה מתקן, כי הוא נעוץ לטעות שכבר קרתה במערכת שלך, לא למפרט גנרי. באגים מתקבצים באותן אי-הבנות — אזורי זמן, כסף, הרשאות — כך שהסוויטה שגדלה מאירועים אמיתיים הופכת למפה של הצוקים המסוימים שלך. טסט גנרי שנכתב ממפרט לא יתפוס את באג שעון הקיץ שכבר שיחררת פעמיים; רק טסט בשם אותו סוף שבוע יתפוס.\n\nפעמיים בשנה אחת, חותמות הזמן של הזמנות אוק זזו יום ללקוחות בבריטניה סביב שעון קיץ. תיקון ראשון: `moment(date).utc()`. תיקון שני, אחרי שסוכן ״ניקה תאריכים״: `new Date(row.created)`. ביום ראשון האחרון של מרץ, 890 הזמנות עברו לאתמול בלוח התפעול; 40 ליקוטים במחסן הלכו לגל הלא נכון; דמי שליח באותו יום של ₪9,400 הוצאו על חבילות שלא היו באיחור עד שהדשבורד אמר שהן כן. לא היה טסט בשם `dst-uk-order-stays-on-the-day`. היו 200 טסטים להדר של ריאקט.\n\nכשבאג מתוקן, ההודעה הבאה לסוכן היא: תכתוב טסט שנכשל ומשחזר את זה, ואז את התיקון. תשאיר את הטסט. תן לו שם לפי האירוע. המלכודת היא לתקן בלי לנעוץ, ואז לתת לריפקטור הבא — לעיתים קרובות של אותו מודל — להחיות אותו. המלכודת השנייה היא ״סוויטת רגרסיה״ ענקית שהיא פשוט סוויטת היוניט עם שם חדש, בלי קישור לכשל עבר. אם אי אפשר להצביע על כרטיס, תאריך ומספר, זה עוד לא טסט רגרסיה. זה תקווה."},

"Coverage": {
 "en": "Coverage is a map of which lines ran, not of which facts were checked. A file at 0% is a real finding if that file charges cards. A file at 100% with assertions that only look at HTTP 200 is a costume. Used as a lamp, coverage is useful: you see the refund function never entered. Used as a target — \"must be 90%\" — teams write tests that execute lines and ask nothing, and the suite gets slower while safety stays flat. The number is a lamp. The moment it becomes a KPI, people will light the lamp and stop looking at the room.\n\nLedger had a CI gate at 94%. `refund()` was \"covered\" because a test called it and expected not to throw. The function did `await stripe.refunds.create` and then `await stripe.refunds.create` again in a retry without an idempotency key. Twelve double refunds over a month, ₪28,600 out the door, noticed by a customer who emailed \"I got paid twice\". Coverage on `billing/refund.ts` was 100%. The uncovered bit was the idea. After they switched the gate to coverage on changed lines plus a mutation-style review of assertions, the next agent PR that added a loop without checking row counts failed the human, not the percentage.\n\nRead coverage as a heatmap, then spend time on auth, money, and delete paths that are cold. Ask the agent: which lines in this diff have no test that would fail if they were deleted? The trap is buying 100% with `istanbul ignore` comments and tests named `it('works')`. The other trap is refusing to merge a comment-only change because the number dipped 0.2%. Chase the risky unvisited path. A 70% suite that pins refunds, IDOR and empty carts beats a 98% suite that walks getters.",
 "he": "כיסוי הוא מפה של אילו שורות רצו, לא של אילו עובדות נבדקו. קובץ ב-0% הוא ממצא אמיתי אם הקובץ הזה מחייב כרטיסים. קובץ ב-100% עם assertions שמסתכלים רק על HTTP 200 הוא תחפושת. בשימוש כמנורה, כיסוי מועיל: רואים שפונקציית הזיכוי מעולם לא נכנסה. בשימוש כיעד — ״חייב 90%״ — צוותים כותבים טסטים שמריצים שורות ולא שואלים כלום, והסוויטה נהיית איטית בזמן שהבטיחות נשארת שטוחה. המספר הוא מנורה. ברגע שהוא הופך ל-KPI, אנשים ידליקו את המנורה ויפסיקו להסתכל על החדר.\n\nללדג׳ר היה שער CI ב-94%. `refund()` היה ״מכוסה״ כי טסט קרא לו וציפה שלא לזרוק. הפונקציה עשתה `await stripe.refunds.create` ואז שוב `await stripe.refunds.create` בניסיון חוזר בלי מפתח אידמפוטנטיות. שנים עשר זיכויים כפולים בחודש, ₪28,600 יצאו, התגלו על ידי לקוח שמייל ״קיבלתי כסף פעמיים״. הכיסוי על `billing/refund.ts` היה 100%. החלק הלא מכוסה היה הרעיון. אחרי שעברו לשער של כיסוי על שורות שהשתנו ועוד סקירה בסגנון מוטציה של assertions, ה-PR הבא של סוכן שהוסיף לולאה בלי לבדוק ספירת שורות נכשל אצל האדם, לא באחוז.\n\nתקרא כיסוי כמפת חום, ואז תשקיע זמן בנתיבי הזדהות, כסף ומחיקה שקרים. תשאל את הסוכן: אילו שורות בדיף הזה אין להן טסט שהיה נכשל אם הן היו נמחקות? המלכודת היא לקנות 100% עם הערות `istanbul ignore` וטסטים בשם `it('works')`. המלכודת השנייה היא לסרב למזג שינוי של הערות בלבד כי המספר ירד ב-0.2%. תרדוף אחרי הנתיב המסוכן שלא ביקרו בו. סוויטה של 70% שנועצת זיכויים, IDOR ועגלות ריקות מנצחת סוויטה של 98% שהולכת על getters."},

"Flaky test": {
 "en": "A flaky test is one that is red and green on the same commit. That is almost never cosmic rays. It is time (`sleep(500)` racing a network), order (test B needs test A's row), or shared state (a singleton cache, a clock, a port). Flakes are often real races wearing a CI costume: the same timing bug will hit a customer, just less often. The cultural damage is worse than the technical one. Once red means \"run it again\", red means nothing, and the suite is a ritual.\n\nPlaywright at Knitco clicked Save and `await sleep(500)` then looked for a toast. Locally, fine. In CI, 1 in 7 runs on `main` failed. The team learned to retry. The same race — POST /save finishing after the assertion — hit production as double-create on slow 3G: last SKU sold twice, 11 times in a flash sale, ₪4,800 in oversell and apologetic overnight shipping. Replacing sleep with `expect(locator).toHaveText('Saved', {timeout: 5000})` made the test wait for the fact, not the clock. Failures dropped to 0 in 400 runs. The oversell needed a database constraint too; the flake had been the smoke alarm they muted.\n\nIf a test is flaky, fix it or delete it the same day. Ask the agent to remove sleeps, to isolate data per test, to wait on conditions. The trap is `retries: 3` in the config as a solution — you have taught CI to ignore your product. The other trap is blaming parallelism and then running everything serially for 40 minutes. Look at the failure: timestamps, port already in use, unique constraint on a fixed email `test@x.com`. Those are the three usual corpses. A red test that is sometimes wrong is more expensive than no test.",
 "he": "טסט הפכפך הוא כזה שאדום וירוק על אותו קומיט. זה כמעט אף פעם לא קרני קוסמוס. זה זמן (`sleep(500)` רץ מול רשת), סדר (טסט B צריך את השורה של טסט A), או מצב משותף (קאש סינגלטון, שעון, פורט). הפכפכות היא לעיתים קרובות רייס אמיתי בתחפושת CI: אותו באג תזמון יפגע בלקוח, פשוט פחות פעמים. הנזק התרבותי גרוע מהטכני. ברגע שאדום אומר ״תריץ שוב״, אדום לא אומר כלום, והסוויטה היא טקס.\n\nPlaywright בקינטקו לחץ שמירה ו-`await sleep(500)` ואז חיפש טוסט. מקומית, בסדר. ב-CI, 1 מ-7 הרצות על `main` נכשלה. הצוות למד לנסות שוב. אותו רייס — POST /save שמסתיים אחרי ה-assertion — פגע בפרודקשן כיצירה כפולה על 3G איטי: ה-SKU האחרון נמכר פעמיים, 11 פעמים במבצע בזק, ₪4,800 של עודף מכירה ומשלוח לילה מתנצל. החלפת השינה ב-`expect(locator).toHaveText('Saved', {timeout: 5000})` גרמה לטסט לחכות לעובדה, לא לשעון. הכשלים ירדו ל-0 ב-400 הרצות. עודף המכירה היה צריך גם אילוץ בדאטהבייס; ההפכפך היה אזעקת העשן שהם השתיקו.\n\nאם טסט הפכפך, תתקן או תמחק באותו יום. תבקש מהסוכן להוריד sleeps, לבודד דאטה לכל טסט, לחכות על תנאים. המלכודת היא `retries: 3` בקונפיג כפתרון — לימדת את ה-CI להתעלם מהמוצר שלך. המלכודת השנייה היא להאשים מקביליות ואז להריץ הכל סריאלית 40 דקות. תסתכל על הכשל: חותמות זמן, פורט תפוס, אילוץ ייחודיות על אימייל קבוע `test@x.com`. אלה שלוש הגופות הרגילות. טסט אדום שלפעמים שגוי יקר יותר מאין טסט."},

"Test-first (TDD)": {
 "en": "Test-first means you write the failing claim before the code that should satisfy it. The red test is a specification in a language the machine can run. Then you make it green, then you tidy. The benefit is not moral. It is that you stated the behaviour in observable terms before falling in love with an implementation. With an agent in the loop it becomes a control: the model receives a test it did not write the passing code for, and you have a binary signal on what comes back. The failing test is cheaper than a paragraph of requirements, because it cannot shrug and say \"looks reasonable\".\n\nA founder asked an agent to \"add tests after you finish checkout\". The model wrote tests that matched the code, including `expect(order.total).toBe(cart.subtotal)` — which omitted VAT, matching the bug. All green. First real UK order undercharged 20%, ₪2,400 on day one, 60 orders. The next week they inverted the ritual: wrote `expect(total).toBe(subtotal + vat)` first, watched it fail, then let the agent implement. The second implementation passed the test and the finance recon. Time to the failing test: four minutes. Time spent arguing with a green suite that encoded the mistake: a full day.\n\nHand the agent the failing test (or write it yourself — it is often shorter than the prompt). Say: do not change the test; make it pass; then show the diff. The trap is tests generated afterwards that photograph the bug. The other trap is a huge design-up-front suite that specifies implementation details (`toHaveBeenCalledWith(thisMock)`), so any good refactor is red. Specify outcomes: status, cents, who owns the row. If the test cannot fail, you are not doing TDD. You are journaling.",
 "he": "טסט-קודם אומר שכותבים את הטענה שנכשלת לפני הקוד שאמור לספק אותה. הטסט האדום הוא מפרט בשפה שהמכונה יכולה להריץ. אחר כך הופכים לירוק, אחר כך מסדרים. היתרון אינו מוסרי. הוא בזה שניסחת את ההתנהגות במונחים נצפים לפני שהתאהבת במימוש. עם סוכן בלולאה זה הופך לשליטה: המודל מקבל טסט שהוא לא כתב עבורו את הקוד שעובר, ויש לך אות בינארי על מה שחוזר. הטסט שנכשל זול יותר מפסקת דרישות, כי הוא לא יכול להרים כתפיים ולהגיד ״נראה סביר״.\n\nמייסד ביקש מסוכן ״להוסיף טסטים אחרי שתסיים צ׳קאאוט״. המודל כתב טסטים שהתאימו לקוד, כולל `expect(order.total).toBe(cart.subtotal)` — שהשמיט מע\"מ, והתאים לבאג. הכל ירוק. הזמנת UK אמיתית ראשונה חייבה חסר 20%, ₪2,400 ביום הראשון, 60 הזמנות. בשבוע שאחרי הם הפכו את הטקס: כתבו קודם `expect(total).toBe(subtotal + vat)`, ראו שזה נכשל, ואז נתנו לסוכן לממש. המימוש השני עבר את הטסט ואת התאמת הכספים. זמן עד לטסט שנכשל: ארבע דקות. זמן שאבד בוויכוח עם סוויטה ירוקה שקידדה את הטעות: יום שלם.\n\nתמסור לסוכן את הטסט שנכשל (או תכתוב אותו בעצמך — הוא לעיתים קצר מהפרומפט). תגיד: אל תשנה את הטסט; תגרום לו לעבור; ואז תרים את הדיף. המלכודת היא טסטים שחוללו אחר כך ומצלמים את הבאג. המלכודת השנייה היא סוויטת תכנון-מראש ענקית שמפרטת פרטי מימוש (`toHaveBeenCalledWith(thisMock)`), כך שכל ריפקטור טוב אדום. תפרט תוצאות: סטטוס, סנטים, מי הבעלים של השורה. אם הטסט לא יכול להיכשל, אתה לא עושה TDD. אתה כותב יומן."},

"Pull request & code review": {
 "en": "A pull request is a bundle of commits with a door on it: CI must pass, a human must look, then it may enter main. Code review is that look — not a taste test of style, a hunt for the three things that hurt: a deleted check, a changed default, a new dependency. The PR is also how you throw away an agent's afternoon without throwing away main. Small diffs get real review. A 1,800-line dump gets a shrug and a green merge button.\n\nAn agent opened PR #841: \"refactor orders\", 1,847 lines, 22 files. The reviewer approved in four minutes — the description was confident, CI was green, the screenshot of the list page looked fine. Buried in `service.ts` the line `if (order.userId !== req.user.id) throw forbidden()` had been deleted during a \"simplify handler\" pass. Six days in production. A partner script enumerated `/orders/{id}` and pulled 8,400 foreign orders. The diffstat had been on the screen. Nobody read past the CSS. Afterward they set a 400-line soft cap and a checklist: ownership, money, secrets. The next agent PR of 90 lines got an actual no on a default timeout change from 2s to 30s.\n\nBefore you merge anything an agent wrote, read `git diff`, not the essay at the top. Ask: what did it delete? Did it add a package? Did a number change? The trap is approving because CI is green — CI cannot see IDOR if no test asserts 403. The other trap is rubber-stamping because the model opened the PR and the model is polite. Review is the job. If the diff is too big to read, the ask was too big: split, or reject. A branch you can delete is cheap. A merge you cannot unread is the whole company for an afternoon.",
 "he": "Pull request הוא חבילת קומיטים עם דלת עליה: CI חייב לעבור, אדם חייב להסתכל, ואז זה רשאי להיכנס ל-main. סקירת קוד היא המבט הזה — לא מבחן טעם של סגנון, ציד אחרי שלושת הדברים שכואבים: בדיקה שנמחקה, ברירת מחדל שהשתנתה, תלות חדשה. ה-PR הוא גם איך זורקים אחר צהריים של סוכן בלי לזרוק את main. דיפים קטנים מקבלים סקירה אמיתית. dump של 1,800 שורות מקבל תנועת כתף וכפתור מיזוג ירוק.\n\nסוכן פתח PR #841: ״ריפקטור הזמנות״, 1,847 שורות, 22 קבצים. הסוקר אישר בארבע דקות — התיאור היה בטוח, CI ירוק, צילום דף הרשימה נראה בסדר. טמון ב-`service.ts` השורה `if (order.userId !== req.user.id) throw forbidden()` נמחקה במהלך ״פישוט מטפל״. שישה ימים בפרודקשן. סקריפט של שותף עבר על `/orders/{id}` ומשך 8,400 הזמנות זרות. ה-diffstat היה על המסך. אף אחד לא קרא מעבר ל-CSS. אחר כך הם שמו תקרת רכות של 400 שורות וצ׳קליסט: בעלות, כסף, סודות. ה-PR הבא של סוכן, 90 שורות, קיבל לא אמיתי על שינוי טיים-אאוט ברירת מחדל מ-2 שניות ל-30.\n\nלפני שאתה ממזג משהו שסוכן כתב, תקרא `git diff`, לא את החיבור למעלה. תשאל: מה הוא מחק? האם הוא הוסיף חבילה? האם מספר השתנה? המלכודת היא לאשר כי CI ירוק — CI לא רואה IDOR אם אין טסט שטוען 403. המלכודת השנייה היא חותמת גומי כי המודל פתח את ה-PR והמודל מנומס. הסקירה היא העבודה. אם הדיף גדול מדי לקריאה, הבקשה הייתה גדולה מדי: לפצל, או לדחות. ברנץ׳ שאפשר למחוק הוא זול. מיזוג שאי אפשר לבטל-קריאה שלו הוא כל החברה לאחר צהריים."},
})


# =====================================================
# Concrete examples. cap is bilingual; code stays English.
# =====================================================

EXAMPLES_FIX = {

"Client": {"cap": {"en": "The server must recompute; the body is a rumour",
                   "he": "השרת חייב לחשב מחדש; הגוף הוא שמועה"},
 "code": "// what the browser posted - a claim, not a fact\nPOST /checkout\n{\"sku\":\"COAT-9\",\"price_cents\":1,\"discount_percent\":100}\n\n// BROKEN - trusts the client\norder.total = body.price_cents\n\n// CORRECT - server is the till\nconst sku = await db.product.find(body.sku)      // 18000 cents\nconst off = coupon.valid ? coupon.percent : 0    // 30, never 100 from body\norder.total = Math.round(sku.price_cents * (100 - off) / 100)\n\n# replay the request with curl; if the price still moves, you have no backend"},

"Server": {"cap": {"en": "One sync call freezes every shopper",
                   "he": "קריאה סינכרונית אחת מקפיאה כל קונה"},
 "code": "// 90ms on your Mac. on Node's one thread it is 90ms nobody else is served.\napp.post('/login', (req, res) => {\n  const hash = bcrypt.hashSync(req.body.password, 12)   // BLOCKS\n  res.json({ ok: true })\n})\n\n// 47 people click Pay -> p99 8.4s, 12% abandon\n\napp.post('/login', async (req, res) => {\n  const hash = await bcrypt.hash(req.body.password, 12) // yields the thread\n  res.json({ ok: true })\n})\n\n// also: AbortSignal.timeout(2000) on every outbound call\n// and never readFileSync / JSON.parse(huge) inside a request"},

"Frontend": {"cap": {"en": "A rate in the component is a rumour on screen",
                     "he": "שיעור בקומפוננטה הוא שמועה על המסך"},
 "code": "// BROKEN - looks right in Figma, wrong in every other country\nconst vat = total * 0.17\n\n// CORRECT - display what the server decided\nconst { vat_cents, total_cents } = await api.quote(cart)\n\n// the three states the screenshot never shows:\nif (loading) return <Skeleton />\nif (error)   return <p role=\"alert\">Could not price this cart</p>\nif (!items.length) return <p>Your cart is empty</p>\n\n// if the number is computed in React, a customer can edit it.\n// if it is missing a loading/error/empty view, they will stare at a lie or a blank."},

"Backend": {"cap": {"en": "The button is not the quota",
                    "he": "הכפתור אינו המכסה"},
 "code": "// UI: hide \"New photo\" at 50. that is decoration.\n\n// BROKEN - the partner integration never saw the button\napp.post('/photos', requireLogin, upload)\n\n// CORRECT - the API is the product\napp.post('/photos', requireLogin, async (req, res) => {\n  const n = await db.photos.count({ sellerId: req.user.id })\n  if (n >= 50) return res.status(403).json({ error: 'photo_quota' })\n  // ... store the file, then increment\n})\n\n# question: delete the frontend tomorrow. what can a session still POST?"},

"API": {"cap": {"en": "Pin the version; parse the failure",
                "he": "לנעוץ את הגרסה; לפרסר את הכשל"},
 "code": "const stripe = new Stripe(process.env.STRIPE_SECRET_KEY, {\n  apiVersion: '2024-11-20',          // pin. do not inherit a snippet from 2020\n  timeout: 2000,\n  maxNetworkRetries: 2,\n})\n\nconst pi = await stripe.paymentIntents.create({\n  amount: order.amount_cents,\n  currency: 'ils',\n  automatic_payment_methods: { enabled: true },\n})\n\n// incomplete is not paid:\nif (pi.status !== 'succeeded') throw new Error(`stripe:${pi.status}`)\n\n# missing field / 429 / timeout: treat as NOT paid, never write status='paid'\n# key lives in the server env, never in the React bundle"},

"localhost & port": {"cap": {"en": "localhost is whoever is asking",
                             "he": "localhost הוא מי ששואל"},
 "code": "# laptop:     http://localhost:3000/api   -> your Node\n# in Docker:   localhost:3000              -> THIS container, not the laptop\n# in a phone:  localhost:3000              -> the phone. nothing is there.\n\n# production frontend must not bake this in at build time:\nVITE_API_URL=http://localhost:3000         # 2,400 phones, connection refused\nVITE_API_URL=https://api.lens.shop         # the actual host\n\n# who is holding 3000?\nlsof -i :3000\nnetstat -ano | findstr :3000\n\n# bind 0.0.0.0 inside Docker so port maps work\n# do not bind 0.0.0.0 on cafe wifi"},

"DNS": {"cap": {"en": "Lower TTL a day before you move",
                "he": "להוריד TTL יום לפני שזזים"},
 "code": "# Sunday 21:00 - you updated the record. your laptop sees CloudFront.\n# Monday 09:00 - 31% of phones still have the old A, TTL 43200\n\ndig shop.example.com +noall +answer\n# shop.example.com.  11844  IN  A  34.120.5.9     <- old VM, still cached\n\n# a day BEFORE cutover:\n#   TTL 300 on the existing record\n# cutover: CNAME shop.example.com -> d111.cloudfront.net\n# AFTER: keep the old origin up read-only until 300s has fully expired\n\n# A record at an ephemeral LB IP will break when the IP moves.\n# CNAME at a hostname the vendor owns will not."},

"Full stack": {"cap": {"en": "One order id, three layers, one price",
                       "he": "מזהה הזמנה אחד, שלוש שכבות, מחיר אחד"},
 "code": "# follow 7c3f from the screen to Stripe. if three places discount, you will stack.\n\n# browser payload:     {\"sku\":\"HAT\",\"discount\":30}\n# API log:             applyCoupon(HAT, 30) -> unit 70\n# Stripe event:        coupon=BF30 on top of 70 -> 49\n# recognised revenue:  49   cost of goods: 110    <-- the incident\n\n# one owner:\nprice_cents = catalog.price_cents(sku)          # server only\nprice_cents = apply(coupon, price_cents)        # server only\nstripe.amount = price_cents                     # no second coupon\n\n# grep the word 'discount' across the repo. more than one writer = the next Black Friday."},

"DevTools": {"cap": {"en": "Five panels, one afternoon",
                     "he": "חמישה פאנלים, צהריים אחד"},
 "code": "F12  /  Ctrl+Shift+I  /  Cmd+Option+I\n\nElements     why does this look wrong?      live HTML + computed CSS\nConsole      what broke?                    errors, REPL on the page\nNetwork      what did it actually send?     status, headers, payload, timing\nApplication  am I logged in?                cookies, localStorage\nSources      what is the value right now?   breakpoints, step, watch\n\n# \"API is slow\" for 3 days: Network TTFB 12ms, Scripting 2.8s\n# the index on products.slug did not matter. the 900-row table did.\n# paste the panel, not a paragraph, into the agent."},

"The Network tab": {"cap": {"en": "Failed in the browser is not a 500 on the server",
                            "he": "נכשל בדפדפן זה לא 500 בשרת"},
 "code": "Filter: Fetch/XHR     [x] Disable cache     [x] Preserve log\n\nName        Status     Time\ncheckout    (failed)   0.03s     <- click it\n\n  Headers  OPTIONS 204, then POST never sent\n  CORS: Access-Control-Allow-Origin: https://shop.oak.co\n        page origin:                 https://b2b.oak.co     <- not on the list\n  Timing   Waiting (TTFB) 0ms   (it never left)\n\n# if Timing.Waiting is 1.19s and Download is 0.05s: your server, not wifi\n# Right-click -> Copy as cURL  -> replay outside the browser"},

"Console & breakpoints": {"cap": {"en": "Pause beats twenty log lines",
                                  "he": "עצירה מנצחת עשרים שורות לוג"},
 "code": "// Console, live page:\n$0                  // selected element\ncopy(cart)          // clipboard\nconsole.table(rows)\n\n// Sources: click line 42 of getOwner -> breakpoint\n// right-click -> Condition: order.id === '7c3f'\n// Watch: cart.items.length\n// Call Stack: how we got here\n\n// this \"fix\" hides the bug:\ncart.items?.id          // crash gone, cart still empty\n\n// the real value at the pause: cart.items = []\n// Application tab: key was cart_v2, deploy renamed it to cart_v3"},

"Typed vs untyped": {"cap": {"en": "Parse at the boundary; never `as`",
                             "he": "לפרסר בגבול; לעולם לא `as`"},
 "code": "// JS - runs, then invoices ₪210\nfunction total(qty, price) { return qty + price }   // \"2\" + 10 = \"210\"\n\n// TS - refuses... unless you lie:\nconst order = await res.json() as Order   // dare, not a check\norder.userId                               // might be a number on the wire\n\n// parse:\nconst Body = z.object({ qty: z.coerce.number().int().positive() })\nconst { qty } = Body.parse(req.body)      // throws -> 400, not a ₪210 invoice\n\n// `any` and `as` in an agent diff = the checker already found a lie"},

"Compiled vs interpreted": {"cap": {"en": "When the typo becomes a customer",
                                    "he": "מתי שגיאת הכתיב הופכת ללקוח"},
 "code": "# Go - 90 seconds after push, still on your laptop\n$ go build ./...\n./invoice.go:88: sendInvioce undefined\n\n# Python - eleven days later, 00:07 on the 1st\nNameError: name 'sendInvioce' is not defined     # 14,000 invoices skipped\n\n# TypeScript with the check turned off is Python in a costume:\n#   ts-node --transpile-only     <- Docker CMD, tsc never ran\n# CI that matters:\n#   go test ./...\n#   tsc --noEmit\n#   pytest                       # starting the process is not a check"},

"JavaScript / TypeScript": {"cap": {"en": "Types are gone on the wire",
                                    "he": "הטיפוסים נעלמים על החוט"},
 "code": "// what you wrote\nexport type Order = { id: string; paidAt: Date }\nexport function isPaid(o: Order) { return o.paidAt.getFullYear() >= 2026 }\n\n// what runs, and what JSON actually is\n{\"id\":\"7c3f\",\"paidAt\":\"2026-03-04T10:00:00.000Z\"}   // a STRING\no.paidAt.getFullYear()   // throws in the worker, works in the browser\n\nconst o = OrderSchema.parse(await res.json())       // zod, at the boundary\nif (pi.status !== 'succeeded') markUnpaid(order.id) // incomplete is not paid\n\n// start in .ts. share the schema. never as Order."},

"Python": {"cap": {"en": "Pin it, venv it, smoke-test the job",
                   "he": "לנעוץ, venv, טסט עשן לג׳וב"},
 "code": "# requirements.txt - the date CI ran should not pick this for you\npandas==2.2.2\nrequests==2.32.3\n\n$ python -m venv .venv && source .venv/bin/activate\n$ pip install -r requirements.txt\n\n# the unpinned version emailed a 0-row report at 07:00\nassert len(report) > 0, 'monthly recon empty'    # 20s in CI, 6h in finance\n\n# mypy on the helper that returned dict | None:\ndef load(id: str) -> Order: ...\n# skip: except Exception: pass   # nightly job writes empty rows instead of dying"},

"Go / Rust / Java / C#": {"cap": {"en": "The repo tells you the error style",
                                  "he": "הריפו אומר לך את סגנון השגיאות"},
 "code": "// Go    - errors are values; swallowing them is a bug in THIS language\nrow, err := db.GetOrder(id)\nif err != nil { return fmt.Errorf(\"get order %s: %w\", id, err) }\n\n// Rust   - will not build if you ignore it\nlet row = db.get_order(id)?;\n\n// Java / C# - exceptions, plus a framework that owns the folders\n\n// what to expect when you open the repo:\ngo.mod      -> one binary, explicit err\nCargo.toml  -> perf, slow review, borrow checker\npom.xml     -> Spring conventions win over the tutorial\n*.csproj    -> same, Microsoft stack\n\n# 4ms Rust thumbnails vs 11 images/min: measure traffic before you rewrite"},

"Package manager": {"cap": {"en": "The range is a wish; the lock is what ran",
                            "he": "הטווח הוא משאלה; ה-lock הוא מה שרץ"},
 "code": "// package.json - may drift\n\"stripe\": \"^12.1.0\"\n\n// package-lock.json - commit this, install THIS in CI\n\"stripe\": { \"version\": \"12.1.0\", \"integrity\": \"sha512-...\" }\n\nnpm ci        # lock file exactly. use in CI\nnpm install   # may rewrite the lock; this is how 12.9.0 arrived in June\n\n# a one-line dependency with a 1,840-line lock diff brought friends.\n# read the lock diff. refuse * and latest."},

"Dependency": {"cap": {"en": "14 direct, 1,190 installed",
                       "he": "14 ישירות, 1,190 מותקנות"},
 "code": "$ npm ls --all | wc -l\n1190\n\n$ npm view is-email-valid time.modified\n'2021-04-02T08:12:44.000Z'     # four years = abandoned, not stable\n\n// own this instead:\nconst ok = /^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(email)\n\n# three date libraries in one checkout bundle:\nmoment   date-fns   dayjs      # keep one, delete two\n\n# agent added a name: what does it do, how many came with it, last release?"},

"Framework vs library": {"cap": {"en": "Who owns the folders",
                                 "he": "מי הבעלים של התיקיות"},
 "code": "// LIBRARY - you call it\nimport { format } from 'date-fns'\nformat(order.createdAt, 'dd/MM/yyyy')\n\n// FRAMEWORK - it calls you; the folder IS the router\napp/orders/[id]/page.tsx     -> GET /orders/:id\napp/orders/loading.tsx       -> shown automatically\n\n// tax: mixing both routers in one repo\npages/api/checkout.ts        // old\napp/checkout/page.tsx        // new  -> 40% 404 after deploy\n\n// pick a building OR drawers. do not put a shed in someone else's kitchen."},

"Semantic versioning": {"cap": {"en": "A major bump is the PR, not a side effect",
                                "he": "קפיצת מייג׳ר היא ה-PR, לא תופעת לוואי"},
 "code": "  1   .   4   .   2\nmajor   minor   patch\nbreaks  adds    fixes\n\n\"^1.4.2\"  -> >=1.4.2 <2.0.0     default, plus a lockfile plus npm ci\n\"~1.4.2\"  -> >=1.4.2 <1.5.0\n\"1.4.2\"   -> exactly\n\"*\"       -> the Friday publish that took down staging Saturday\n\n# agent changed \"zod\": \"3.22.4\" to \"^4.0.0\" while \"updating deps\"\n# 14 schemas failed at runtime, 47 minutes of checkout 500s\n# changelog first, then the bump, alone."},

"Test": {"cap": {"en": "Would this have caught the empty-cart 200?",
                 "he": "האם זה היה תופס את ה-200 על עגלה ריקה?"},
 "code": "test('empty cart is 400, not a phantom order', async () => {\n  const res = await api.post('/checkout', { items: [] }).as(user)\n  expect(res.status).toBe(400)                 // the line that would have saved Monday\n  expect(await db.orders.count()).toBe(0)\n})\n\n// the \"fix\" without this test:\n//   res.json({ id: null, items: [] })  status 200\n//   31 pick-lists, no SKU, three pickers walking aisles\n\n// if you only write three: ownership, money, deletion"},

"Assertion": {"cap": {"en": "Assert the 403, not the 200",
                      "he": "לטעון את ה-403, לא את ה-200"},
 "code": "// 39 of 40 tests:\nexpect(res.status).toBe(200)     // passes while B reads A's order\n\n// the one that is the test:\nconst a = await createUser(), b = await createUser()\nconst order = await createOrder({ userId: a.id })\nconst res = await api.get(`/orders/${order.id}`).as(b)\nexpect(res.status).toBe(403)\nexpect(res.body.id).toBeUndefined()\n\n// jump to expect() in an agent file.\n// status 200 / length > 0 / not.toThrow = costume coverage"},

"Unit / Integration / E2E": {"cap": {"en": "The unique email lived in a type, not in Postgres",
                                     "he": "האימייל הייחודי חי בטיפוס, לא בפוסטגרס"},
 "code": "// unit: fast, no database. will not catch this.\nfunction normalise(email: string) { return email.trim().toLowerCase() }\n\n// integration: real Postgres in CI\nawait api.post('/signup', { email: 'a@x.com' })\nconst dup = await api.post('/signup', { email: 'a@x.com' })\nexpect(dup.status).toBe(409)     // 2,100 dupes in 3 weeks without this\n\n// e2e: one boring path that makes money\nawait page.fill('#email', 'a@x.com')\nawait page.click('#pay')\nawait expect(page.getByText(/order 7c3f/i)).toBeVisible()\n\n// 400 units + 0 integration = a type that is not a constraint"},

"Mock / Fixture": {"cap": {"en": "Mock Stripe; do not mock the till",
                           "he": "לזייף Stripe; לא לזייף את הקופה"},
 "code": "// YES - you cannot run Stripe in CI\nvi.mock('./stripe', () => ({ charges: { create: vi.fn().mockResolvedValue({id:'ch_1'}) } }))\n\n// ALSO YES - unsigned webhook is 400 (fixture = real-shaped payload)\nconst res = await api.post('/webhooks/stripe', body, { 'stripe-signature': 'nope' })\nexpect(res.status).toBe(400)\n\n// NO - now you are testing the mock\nvi.mock('./db')\n\n// 96% coverage, ₪67,000 unmarked captures: the mock always said paid"},

"Regression": {"cap": {"en": "Name the test after the incident",
                       "he": "לתת לטסט את שם האירוע"},
 "code": "// dst-uk-order-stays-on-the-day.spec.ts\ntest('UK order created on DST weekend stays on that calendar day', () => {\n  // 890 orders shifted last March. do not \"clean up dates\" without this.\n  const row = parseTs('2026-03-29T01:30:00+00:00')\n  expect(londonDay(row)).toBe('2026-03-29')\n})\n\n// agent \"cleanup\" revived: new Date(row.created)\n// pin the bug in the same hour you fix it, or it returns with a different helper"},

"Coverage": {"cap": {"en": "100% of refund.ts, 12 double payouts",
                     "he": "100% על refund.ts, 12 תשלומים כפולים"},
 "code": "// covered: the function was CALLED\nawait refund(orderId)\nexpect(true).toBe(true)          // 100% on billing/refund.ts\n\n// checked: the thing you would shout about\nawait refund(order.id)\nawait expect(refund(order.id)).rejects.toThrow(/already_refunded/)\nexpect(await stripe.refunds.list({charge})).toHaveLength(1)\n\n# heatmap: auth / money / delete at 0% is a finding\n# 94% with ignore comments is a costume\n# ask: which deleted line in this diff would still leave the suite green?"},

"Flaky test": {"cap": {"en": "Wait for the toast, not the clock",
                       "he": "לחכות לטוסט, לא לשעון"},
 "code": "// flaky: 1 in 7 on CI, muted with retries:3, then oversold 11 SKUs\nawait page.click('#save')\nawait sleep(500)\nexpect(await page.text('.toast')).toBe('Saved')\n\n// stable: wait for the fact\nawait page.click('#save')\nawait expect(page.locator('.toast')).toHaveText('Saved', { timeout: 5000 })\n\n// other corpses: port in use, unique email 'test@x.com' shared across tests\n// fix or delete the same day. retries teach everyone that red means nothing"},

"Test-first (TDD)": {"cap": {"en": "The test is the spec you hand the model",
                             "he": "הטסט הוא המפרט שמוסרים למודל"},
 "code": "// 1. write this FIRST. watch it fail. do not let the agent edit it.\ntest('total includes VAT', () => {\n  expect(total({ subtotal: 10000, vat_bps: 2000 })).toBe(12000)  // 20%\n})\n\n// 2. prompt: make this pass. do not change the test. show the diff.\n\n// tests-after photographed the bug:\n//   expect(order.total).toBe(cart.subtotal)   // green, 60 UK orders undercharged\n\n// if the test cannot fail, you are journaling"},

"Pull request & code review": {"cap": {"en": "Read the diff, not the essay",
                                       "he": "לקרוא את הדיף, לא את החיבור"},
 "code": "git diff --stat     # 1,847 lines / 22 files = reject or split\ngit diff            # hunt these, every time:\n\n-  if (order.userId !== req.user.id) throw forbidden()   // deleted check\n-  const TIMEOUT_MS = 2000                                // changed default\n+  \"lodash\": \"^4.17.21\"                                  // new dependency\n\n# CI green does not see IDOR. a 4-minute approve on PR #841 shipped 6 days of it.\n# if you cannot read it, the ask was too big."},
}

HE_FIX = {}
