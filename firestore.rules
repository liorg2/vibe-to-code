rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // each signed-in user owns exactly one progress doc, and nobody else can touch it
    match /progress/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
